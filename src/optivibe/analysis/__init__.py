"""Analysis package: cross-sensitivity, sweeps and metrics (lands in S6).

This package will host the post-run analytics required by document 00 and the
simulation spec (11 §4): cross-axis sensitivity maps, frequency sweeps and
linearity/full-scale studies. In S0 it is an empty namespace so the import graph
and packaging are in place; nothing here is wired into the pipeline yet.
"""

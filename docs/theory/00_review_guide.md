# 00 — Входной приёмочно-обзорный документ (Review & Onboarding Guide)

**Проект:** OptiVibe — цифровой двойник волоконно-оптического датчика (ВОД) вибрации.
**Кому:** стороннему опытному физику, получившему репозиторий и физбазу.
**Что это:** единый **навигационный** документ приёмочного слоя. Он **не выводит физику заново** —
он связывает существующие артефакты (физбаза 00–08, слой `docs/theory/` 01–05, решения 06/13,
код и тесты) сквозной тройной ссылкой **«вывод → решение → тест»** и даёт воспроизводимый
маршрут «проверь сам». Первоисточник любого числа — физбаза 00–08 и код; здесь только карта и
приёмка (принцип слоя — 15 §9.3, `docs/theory/README.md`).

**Как читать.** Разделы 1–5 отвечают на четыре вопроса физика по порядку: *что за модель* (§2) →
*верна ли модель* (§3) → *верен ли ей код и сходятся ли расчёты* (§4) → *как этим пользоваться*
(§6) → *каковы границы перед реальным датчиком* (§7). Ядро — §5 «Проверь сам».
Обозначения — по 01; идентификаторы/пути — как в коде (`main`, HEAD `25e2b9d`).

---

## 0. Рамка зрелости (прочитать первым)

Три уровня согласия по словарю 17 §3. Честный статус на момент документа:

| Уровень | Что сравнивается | Статус | Где смотреть |
|---|---|---|---|
| **L1** — код ↔ аналитика | функция кода ↔ замкнутая формула базы 02/03/05/07 | **пройден** | план **18** (golden-инвентарь, дыры G1–G12) |
| **L2** — модель ↔ модель | кросс-проверки 17 §2, предельные случаи, межмодельные тождества | **пройден** | план **19** §2 |
| **L3** — модель ↔ стенд | предсказание двойника ↔ измерение прибора | **НЕ пройден — только качественно спланирован** | план **19** §3, план **20** |

**Почему L3 не пройден (без умолчаний):** фаза 0 не измерена (характеризация прототипа Ф0-1…Ф0-9,
20 §1); железо не выбрано (дорожка закупки 20 §6); конфигурация двойника под прототип
`configs/variants/proto_poc.yaml` **содержит плейсхолдеры** (R_c, A, L, P — до D-01…D-03; 14; 06 R-44).
Всё, что ниже помечено «замысел», **не** имеет реального железа / firmware / АЦП-тракта / реального
времени (`docs/theory/README.md`; план 20).

**Разметка по всему документу:**

- **[реализовано]** — смоделировано и есть в коде (указан модуль/функция, пиннится тестом);
- **[замысел]** — проектный замысел / дорожная карта (ссылка на 07/08/20), кода нет.

---

## 1. Как устроен продукт и этот документ *(навигация)*

Три слоя документации, от первоисточника к продукту (разграничение — 15 §1/§9.1):

1. **Физбаза 00–08** — первоисточник чисел, выводов и решений (журнал 06). Здесь принимаются
   физрешения `R-..`.
2. **Слой `docs/theory/` 01–05** — поблочные **выводы** с проверками (размерность / пределы /
   литература), числа воспроизводятся из моделей кода, каждая формула привязана к функции. Порядок
   `01…05` — по ходу сигнала; сквозной `usage_modes.md`. Шаблон блока — семь разделов
   (`docs/theory/README.md`).
3. **Руководство `docs/manual/`** — продуктовое «куда нажать и зачем» (стандарт 15 §1–§8).

Карты навигации, которые этот документ **надстраивает, а не дублирует:** `docs/index.md`
(обзор ПО), `docs/physics-map.md` (таблица «документ базы → код»), `docs/reference.md`
(автогенерируемая API-справка mkdocs). Настоящий документ — их **приёмочный слой**: «проверь
корректность и научись пользоваться».

> **Тройная ссылка.** Ключевое утверждение сопровождается тремя якорями: **вывод** (theory §… /
> база 02–08) → **решение** (06 `R-..` или 13 `SW-..`) → **тест** (`tests/…::…`, пиннящий формулу).
> Это и есть механизм проверяемости: не «нам сообщают, что верно», а «вот вывод, вот запись
> решения, вот тест, который сломается при тихой правке».

---

## 2. Физическая модель — что моделируется *(цель a: понять модель)*

Геометрия: волокно-консоль (заделка `z=0`, свободный торец `z=L`); свет по волокну → воздушный
зазор `A` → выпуклый отражатель (v1 — цилиндр) → возврат в волокно → фотодетектор. Состояние
торца — вектор интерфейса `q_tip = (Δx, Δy, Δz, θx, θy)`, `Δz` — изменение зазора (06 R-04; СК и
знаки — 01). Целевая ось `x` (плоскость кривизны цилиндра), `y` — «плоская», образующая ∥ `y`
(06 R-11).

Цепочка сигнала и её якоря (вывод — theory; репер — база; решение — 06):

| Звено | Ключевой результат | Вывод (theory / база) | Решение (06) |
|---|---|---|---|
| Механика консоли | `f₁ ≈ 100/L²` кГц; `H_lat^QS ≈ 0.0384·L⁴` нм/g; жёсткая связь `θ = 1.377·Δ/L` | `01_sensing_element`; 02 §8 | R-07, R-10 |
| Динамика | `\|D(f)\|`: `f→0 ⇒ 1`, `f=f₁ ⇒ Q`; режим **внерезонансный** (квазистатическая полка) | `01_sensing_element`; 05 | R-21 |
| Оптика (обратная связь) | `η(Δx,Δy,Δz)`; `∂η/∂Δy = 0` в 1-м порядке; `∂η/∂Δz` — трёхчленная форма, `≈ −7.9·10³` м⁻¹ | `02_optical_path`; 03 §e (v1.2), 04 §4 (v1.1) | **R-41** |
| Модель сигнала | интенсивностная `S = ℛP(R₁ + ρη)`; маршрут 2 (когерентный смыв, `V<0.03`) | `03_photodetection`; 03, 05 | R-13, R-14, **R-19** |
| Сквозная чувствительность | `s_target(f) = ℛ·P·ρ·(∂η/∂Δx)_эфф·H_QS·\|D(f)\|` | `05_software_firmware`; 05 §2 | R-19, R-20 |
| Шум/предел | `NEA(f) = i_n/\|s_target\|`, квадратурно с `NEA_th`; «ускорительная» масса `M_a ≈ 0.58ρSL` | `03_photodetection`; 07 §1–§2 | **R-22, R-25** |
| Направленность | анизотропия `≈ L/(1.377·R_c)` → перекрёстная ось | `02_optical_path`; 04 §6 | R-43 |
| Семейство A/B/C/D | мастер-закон `NEA ∝ f_max²`; эталон численных примеров — **B** (`L=2.0` мм, `R_c=31` мкм, `A=31` мкм, `Δx₀=0.992` мкм) | `usage_modes`; 08 | R-42 |

Эталонная точка численных примеров во всём слое — **вариант B** (`docs/theory/README.md`); числа в
блоках воспроизводятся из моделей кода, не из памяти.

---

## 3. Корректность физической модели: уровень **L2** *(цель a/b)*

L2 проверяет **согласованность модели с собой**: производные соотношения 17 §2, предельные случаи
семейства форм, межмодельные тождества. Принцип: метрики **не независимы**; рассогласование между
связанными метриками трактуется как сигнал ошибки, а не «две оценки» (19 §1). Навигатор — 19 §2.1–2.2.
Примеры доказательных точек (все — тройной ссылкой):

- **Мастер-закон `NEA ∝ f_max²`** — связка `NEA∝L⁻⁴` и `f₁∝1/L²`.
  Вывод 08 (R-31) → решение **19 V19-L2-04** → тест `tests/test_model_vv_l2.py::golden_nea_master_law_fmax_squared`.
- **Межмодельные тождества по плоскостям** — `cylinder(силовая) ≡ sphere(x)`; `cylinder(плоская) ≡ plane`,
  `sphere R_c→∞ → plane`. Вывод 03 §c′/§d (канонизирован **R-43**) → решение 19 V19-L2-05/06 →
  тесты `…::golden_cylinder_planes_match_sphere_and_plane` (rtol `1e-12`), `…::golden_sphere_infinite_radius_matches_plane` (`1e-3`).
- **Факторизация `∂η/∂Δz`** — трёхчленное разложение как тождество.
  Вывод 04 §4 v1.1 (**R-41**, аннулирует прежнее `−2·10⁴` м⁻¹) → решение 19 V19-L2-07 →
  тест `…::golden_dz_slope_three_term_decomposition` (тождество `1e-6` + репер `−7.9·10³` м⁻¹, 5 %).
- **Пределы `\|D(f)\|`** — `f→0 ⇒ 1`, `f=f₁ ⇒ Q` (точно), пик `= Q×QS`.
  Вывод 02/05 → тесты `tests/test_mechanics_modal.py::limit_dynamic_factor_dc_is_unity`,
  `…::limit_dynamic_factor_at_resonance`, `…::golden_resonant_peak_is_q_times_qs`.
- **Анизотропия / перекрёстная ось** — `≈ L/(1.377·R_c)` (34.86× @3 мм), остаток `∝ Δy²`.
  Вывод 04 §6 → тесты `tests/test_optics_cylinder.py::golden_anisotropy`, `…::golden_cross_axis_residual_quadratic`.

**Известное ограничение L2 (не дефект):** сквозные L2-проверки восстановления существуют только для
цилиндра — `standard`-инверс **cylinder-only** (граница G9, см. §7); для sphere/plane/wedge L2 держится
на уровне `η` (19 §2.4).

---

## 4. Корректность кода и сходимость: уровень **L1** *(цель b)*

L1 сверяет каждую функцию с замкнутой формулой базы. Навигатор — **18 §3** (таблица «метрика 17 §1 →
тест», статусы по легенде 18 §3: пройдено / закрыто-патчем / через-S-02 / не-моделируется). Допуски L1 (17 §3 / 11 §7): механика/оптика ≤ 2–5 %, шумы ≤ 10 %
(PSD sim↔analytic ≤ 15 %), пределы — точно, `s_target` умолчаний v1 — **бит-в-бит**.

Golden, пиннящие именно **формулы базы** (правило 18 §5(г): golden ссылается на замкнутую формулу
или стандарт, а не на «текущий выход кода»):

- **Таблица зон ISO 10816-3** — все 4 класса, конвенция границы `v ≤ предел → нижняя зона`.
  Стандарт → 18 G1 → `tests/test_dsp.py::test_iso_zone_boundaries_match_published_table`.
- **THD ↔ аналитическая 2-я гармоника** `HD2 = \|η″\|·d/(4\|η′\|)` (Тейлор 2-го порядка).
  Вывод 03 §5 → 18 G4 → `tests/test_optics_cylinder.py::test_golden_thd_matches_analytic_second_harmonic` (2 %, факт 0.3 %).
- **Тождество динамического диапазона** `DR = 20·log₁₀(FS·g₀ / NEA·√B)` для A/B/C/D.
  17 §1 → 18 G2 → `tests/test_analysis.py::test_analytic_point_dynamic_range_identity` (rel `1e-12`).
- **Полнополосный `NEA·√B ↔ ∫NEA(f)²df`** + пиннинг **двух** конвенций полосы (найквистова у
  `NeaBudget` vs проектная у `AnalyticPoint`).
  07 → 18 G3 → `tests/test_analysis.py::test_nea_full_band_consistent_with_spectrum` (≤ 10 %).

**Бит-идентичность A/B/C/D** — контракт `tests/data/resolved_variants_golden.json` (полный `model_dump`
разрешённых вариантов). Правило: меняется **только осознанно и аддитивно**; любое изменение
существующих чисел = смена контракта с записью в 13 (18 §5). Якорь обратного тракта
`static+ideal+plateau ≡ v1` (`s_target = −1.93·10⁻⁷`) — бит-в-бит, `tests/test_sensitivity.py::static_default_is_bit_identical_to_v1`.

**Метрики на реальных данных** — роль «анализатор реального выхода прибора» (запись фототока →
метрики 17 §1 минуя forward), реализована на синтетике (18 G5 закрыт, SW-52):
`tests/test_analysis_instrument.py` (round-trip forward→файл→`analyze`), `tests/test_io_records.py`
(единицы V→A, метаданные). Реальные записи Э-0/Э-3 добавляются аддитивно после стенда.

> **Замечание о счёте golden.** `pytest -m golden` даёт **86** (см. §5). Наивный grep декораторов даёт
> меньше — часть модулей помечена `pytestmark` на уровне файла; методика счёта разъяснена в 18 §2. При
> сверке пользуйтесь маркером, а не grep.

---

## 5. Корешок проверяемости — «проверь сам» *(ядро)*

Ровно тот маршрут, что проходит координация на каждой приёмке. **Корректность проверяема, а не
декларируется.** Ниже — (а) команды рецензенту с ожидаемыми числами (из 18/14) и (б) логи, реально
**воспроизведённые координацией** при подготовке документа, — чтобы рецензент сравнил своё окружение
с эталоном.

### 5.1 Восстановление и ворота

```bash
git clone https://github.com/SuperMarioFernandess/optivibe
cd optivibe && git checkout 25e2b9d
uv sync --locked --python 3.11 --extra dev --extra gui --extra io-formats
export QT_QPA_PLATFORM=offscreen        # pytest-qt/GUI headless (18 §5)

uv run ruff check
uv run ruff format --check
uv run mypy
uv run pytest --cov=optivibe            # полный набор
uv run pytest -m golden                 # подмножество golden
sha256sum tests/data/resolved_variants_golden.json
```

Пин Python **3.11** обязателен: свежий numpy тянет стабы 3.12-синтаксиса, ломающие `mypy` под 3.11 —
`mypy`-истина именно CI на 3.11 (18 §5 «Риски окружения»). Ворота валидны только из окружения с
`--extra dev --extra gui --extra io-formats` (без gui-extra охват `mypy` молча сузится ниже 86).

### 5.2 Доказательные точки (доминанты и роль анализа)

```bash
uv run optivibe run     examples/hello.yaml         # ожидается 120.000 Hz (вариант B)
uv run optivibe run     examples/proto_poc_2f.yaml  # ожидается 240.000 Hz (2f при bias≈0, E1-P1)
uv run optivibe analyze examples/analyze_record.yaml # ожидается 120.000 Hz (роль S-02, 18 G5)
```

`proto_poc_2f → 240 Гц` демонстрирует фальсифицируемое предсказание **E1-P1** (квадратичный отклик у
вершины `η`: подавленная 1f, доминанта на 2f, амплитуда `2f ∝ d²`; вывод 03, решение 06 R-44, тест
`tests/test_model_vv_l2.py::golden_proto_poc_two_f_response`). `analyze` — тот же тракт метрик, что и
forward (общий `StandardDsp.run`, параллельных реализаций нет — 17 §7).

### 5.3 Сборка теории (self-contained HTML, офлайн)

```bash
uv run python docs/theory/tools/fetch_mathjax.py   # однократно: вендорит tex-svg.js (пин версии+sha512)
uv run python docs/theory/tools/build_theory.py    # все блоки → docs/theory/_build/*.html
```

Порядок и вендорение — фикс **SW-54**: `tex-svg.js` (не CHTML — тот догружает шрифты в рантайме);
удалён битый `--mathjax`+`--embed-resources` fallback; при отсутствии вендоренного MathJax — ранний
отказ с инструкцией. Результат и вендоренный файл не версионируются (O-SW-09).

### 5.4 Сводка: ожидание (18/14) ↔ воспроизведено координацией

Прогон: свежий клон **HEAD `25e2b9d`**, `uv --python 3.11` → **Python 3.11.15**, **numpy 2.4.6**,
PySide6 6.11.1, pandoc 3.1.3, `QT_QPA_PLATFORM=offscreen`. **Расхождений с эталоном нет.**

| Точка | Ожидается (18/14) | Воспроизведено координацией |
|---|---|---|
| `ruff check` | чисто | `All checks passed!` |
| `ruff format --check` | чисто | `119 files already formatted` |
| `mypy` (strict, `files=src`) | 86 файлов, 0 ошибок | `Success: no issues found in 86 source files` |
| `pytest` | 417 passed | `417 passed in 16.18s` |
| Покрытие (branch, `gui/` omit) | 91.20 % | `TOTAL … 91.20%` (`coverage report --precision=2`) |
| `pytest -m golden` | 86 | `86 passed, 331 deselected` (86+331=417) |
| `hello` доминанта | 120.000 Гц | `dominant : 120.000 Hz` (`variant=B`) |
| `proto_poc_2f` | 240.000 Гц | `dominant : 240.000 Hz` (`variant=proto_poc`) |
| `analyze_record` | 120.000 Гц | `dominant : 120.000 Hz` |
| sha256 golden json | `29aba1a1…92688` | `29aba1a1…92688` |
| `fetch_mathjax` | вендорит `tex-svg.js` | `tex-svg.js (2 108 580 bytes)` |
| `build_theory` | 6 self-contained HTML | 6 файлов (5×~2.1 МБ), **0** внешних `script/link` |

Если у рецензента число разойдётся — сравнивать в первую очередь версию Python (3.11 vs 3.12,
`mypy`/numpy-стабы) и полноту extras (gui-extra → 86 файлов `mypy`).

---

## 6. Как пользоваться: CLI и GUI *(цель c)*

**CLI** — точка входа `optivibe` (`pyproject.toml [project.scripts]`), подкоманды
`run`, `report`, `sweep`, `analyze` (`src/optivibe/cli/main.py`). Сценарий — YAML
(`{name, variant∈{A,B,C,D,proto_poc}, excitation, stages, dsp, seed, output}`; 13 SW-11). Коды выхода:
`0` — успех, `2` — ошибка входа. Приёмочные доминанты (полный набор регресс-защищён — 18 §5):
`hello 120`, `replay_tdms 50`, `cross_axis 240`, `resonance_sweep 5005`, `analyze 120`, `proto_poc_2f 240`.
Продуктовое описание — `docs/manual/` (модули `ui_cli`, `ui_data`), API — `docs/reference.md`. **[реализовано]**

**GUI** — десктоп PySide6 + PyQtGraph (`optivibe-gui`), 5 вкладок; редактируемая композиция подсистем
(форма на подсистему, пресет + SI-overrides), вкладка «Физмодели/справка» (лёгкие `f₁(L)`/`\|H_lat(f)\|`/
`η(Δx)` inline, тяжёлый `NEA(f)` — через воркер). Тяжёлые расчёты — в `QThread`-воркерах, GUI-поток не
блокируется; **headless-паритет:** любой прогон доступен и через `cli` (13 SW-06/SW-13/SW-45; 14 §1).
Продуктовое описание — `docs/manual/ui_gui`. **[реализовано]**

**Импорт данных.** Две роли (13 SW-08): (а) **replay-вход** — эталонный канал ускорения через
загрузчики `LOADER_REGISTRY` (csv, wav, tdms, uff, mat, hdf5); (б) **анализ выхода прибора** —
`optivibe analyze` (роль S-02): запись фототока → метрики минуя forward. Приведение единиц к СИ и
метаданные — 20 §5; загрузчики — `src/optivibe/io/`. **[реализовано]** (реальных записей прибора пока
нет — только синтетические round-trip'ы; §7).

---

## 7. Границы применимости и зрелость *(цель d)*

**Границы ядра (зафиксированы как решения, не баги):**

- **G9 — `standard`-инверс cylinder-only.** Калиброванная обратная задача (static/operating_point/
  nonlinear) реализована только для цилиндра (`CylinderOpticsModel`); sphere/plane/wedge прогоняются со
  `stub`-DSP или обходятся явной калибровкой `measured`/`bench`. Для не-цилиндра с источником `model`
  анализатор **громко отказывает** (не молчит). 18 G9, 14 §1, 19 §2.4.
- **MG-1 / кандидат M-12 — тепловой пол `NEA_th` не в коде.** Квадратурное слагаемое `NEA_th` (07 §2,
  `NEA_th=√(4k_BTω₁/(Q·M_a))`) не реализована ни в `detector`, ни в `analysis/nea_budget`. Для
  **B/C/прототипа** пренебрежимо (`<0.2 %`), для **A/D** (тепл.-лимитные) L3-сверка NEA будет
  систематически занижать модельный шум. Эскалировано в бэклог. 19 §2.1 (V19-L2-09)/§5, 06 R-44.

**Немоделируемое в v1** (L2/L3-проверок для этого нет — 19 §6): температура TCS/TCB (**M-05**, только
хуки), поляризация/двулучепреломление (**M-09**; схема общего пути устойчива к SOP — принято допущением),
мультимода/мода 2+ (вклад `<0.5 %` в полосе). Нелинейная инверсия `>50g` (M-07) — вне v1.

**Стендовая реализуемость (граница валидации, не модели).** Требование «50g на любой частоте полосы»
дословно нестендуемо: на 0.1 Гц это `x_pk≈1242 м`. Прямой гармонический 50g реализуем в окне
`≈50 Гц – 5 кГц`; НЧ — эквивалентами (центрифуга + перенос линейности), ВЧ 20 кГц — только пьезо +
лазерная виброметрия. Разнесение проверки — 20 §0(а)/§4.6.

**Единственный вскрытый физдефект — пример работающего процесса (не тревога).**
При выводе блока `03_photodetection` слоя `docs/theory/` вскрыта опечатка **единицы** «ускорительной»
массы `M_a` в 07 §2: значение `M_a(1.4 мм)=0.58ρSL=2.19·10⁻⁸ кг` было подписано «≈22 нг», тогда как
это `≈22 мкг` (×1000, «нг» вместо «мкг»). **Значение в СИ и все производные `NEA_th` — верны;**
ошибочна только скобочная подпись. Дефект **не «тихо починен»**, а проведён процессом: зафиксирован
как решение **06 R-45** и внесён в первоисточник 07 координацией; `MG-1` (нереализованность `NEA_th`)
это не затрагивает. Это единственный физдефект за все пять блоков слоя — иллюстрация того, что маршрут
«вывод → решение → тест» работает.

**Перед работой с реальным датчиком.** L3 не пройден — сначала: фаза 0 (характеризация прототипа,
20 §1, снятие плейсхолдеров `proto_poc`), качественные предсказания **E1-P1…P6** (19 §3.2, доступны без
точных R_c/A/L/P), затем количественный каскад по протоколу GUM `k=2`
(`\|X_модель − X_стенд\| ≤ 2·√(u_м²+u_с²)`, 19 §3.3; ворота первой сверки `s_target` — честные ±40 %,
19 §4.2 / 20 §7).

---

## 8. Открытые вопросы и что дальше

- **Очередь верификации:** остаток G5 (реальные записи), G6–G12 — 18 §4.2; L2/L3-каскад — 19 §7;
  закупка и lead time — 20 §6.
- **Куда писать замечания рецензента.** Вскрытый **физпробел** → открытый вопрос координатору и запись
  в **06** (правится в физбазе, не в документации — 15 §9.3). Программное/тестовое замечание → **13**
  (`SW-..`) и бэклог **16**. Метрологическое/стендовое → **20**. Настоящий документ пробелы **не чинит
  молча** — выносит.

---

## Приложение A — карта «цель физика → раздел»

| Цель | Раздел | Основные артефакты |
|---|---|---|
| Оценить корректность **физической модели** | §2, §3 | theory 01–05; база 02–08; 06 R-11/R-19/R-41/R-43; план 19 §2 |
| Убедиться, что **код верен модели** и расчёты сходятся | §4, §5 | план 18; golden-тесты; `resolved_variants_golden.json`; §5 логи |
| Научиться **пользоваться** (CLI/GUI/данные) | §6 | `docs/manual/` (`ui_cli`/`ui_gui`/`ui_data`), `docs/reference.md`, `docs/index.md` |
| Понять **границы** перед реальным датчиком | §7, §0 | 18 G9, 19 §5/§6 (MG-1, M-05/M-09), 20 §0/§7; 06 R-44/R-45 |

## Приложение B — глоссарий

Обозначения и константы **не дублируются**: единый источник — **01** (СК, `q_tip`, константы) и
`docs/theory/*` (по блокам). Метрики V&V (s_target, f₁, Q, NEA, C_xy, DR, THD, B …) — словарь **17 §1**.

---

*Документ навигационный и производный (15 §9.3): при любом расхождении первичны физбаза 00–08, журналы
06/13 и реальный код `main`. HEAD на момент написания — `25e2b9d`.*

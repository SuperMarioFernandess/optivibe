// docs/manual/sections/ui_gui.mjs
// AUTO-SLICED (S-DOC-2) from manual_build_doc.js lines 124..164; content verbatim.
// Owns its bookmarks/links. Edit this module only; build.mjs orders it via manifest.mjs.
import {
  R, c, P, para, H, xr, flink, ext, links, sep, code, cap, bullets, nums, img, tbl, fml,
  AlignmentType, Paragraph, TextRun, PageBreak,
  REPO, FIG, INK, MUT, ACC, ACCD, LINE, CW,
} from "../shared.mjs";

export function section() {
  const K = [];
  K.push(H(1,"3. Взаимодействие с ПО","ch3"));
  K.push(para([R("ПО доступно двумя путями с общим ядром: десктоп-приложение (PySide6 + PyQtGraph) для интерактивной работы и показа, и командная строка (CLI) для воспроизводимых прогонов и автоматизации. Оба используют один и тот же конвейер "), c("pipeline"), R(" и аналитику "), c("analysis"), R(" — поэтому результат идентичен (headless-паритет).")]));

  K.push(H(2,"3.1 Десктоп-приложение (GUI)","s_ui_gui"));
  K.push(para([R("Запуск — командой "), c("optivibe-gui"), R(" (точка входа "), flink("src/optivibe/gui/app.py","gui/app.py"), R("). GUI — тонкая оболочка над ядром: внутри нет физики и DSP, только сбор конфигурации, запуск расчёта в рабочем потоке и отображение результатов.")]));
  K.push(H(3,"Главное окно: панель управления и вкладки","s_ui_window"));
  K.push(para([R("Окно ("), flink("src/optivibe/gui/main_window.py","gui/main_window.py"), R(") состоит из левой панели управления и области вкладок с кнопками действий внизу.")]));
  K.push(tbl(
    ["Элемент окна","Назначение"],
    [
      ["Панель управления (слева)","Композиция датчика в формах подсистем (SystemBuilderPanel; System / Source / Fiber line / Cantilever / Reflector / Detector), плюс вкладки Excitation, Physics layers, DSP experiment и Reproducibility — всего десять. A/B/C/D — стартовые композиции, которые далее правятся; из форм собирается SystemConfig → сценарий. Подробно — §3.4, панель эксперимента — §3.7."],
      ["Вкладка Live","Два режима на одном экране. Разовый прогон: живые графики PyQtGraph — анимация изгиба консоли, вход a(t) против восстановления, сигнал детектора, скорость/перемещение, спектр, NEA(f) с разложением. Непрерывный поток (§3.6): те же графики обновляются кадрами, две панели осознанно пусты, внизу — строка провенанса."],
      ["Вкладка Report","Встроенные фигуры matplotlib: бюджет «истина vs восстановление» (a/v/x), спектр, спектрограмма, NEA(f) с компонентами."],
      ["Вкладка Sweeps","Карты развёрток: NEA и чувствительность по {L, R_c, P, bias, FS, вариант}; отклик/THD по амплитуде 0.1g→>50g."],
      ["Вкладка Monte-Carlo","Гистограммы и боксплоты статистики NEA и перекрёстной чувствительности по разбросам допусков."],
      ["Вкладка Compare","Стенд сравнения цепочек ЦОС: две цепочки на одном входе, наложение a(t) и спектра, таблица расхождений, вердикты обеих цепочек. Подробно — §3.7."],
      ["Вкладка Physics","Физмодели и справка: лёгкие f₁(L), |H_lat(f)|, η(Δx) считаются inline, тяжёлый NEA(f) — через рабочий поток."],
      ["Кнопка Run","Запускает сценарий (forward+inverse) и обновляет вкладку Live."],
      ["Кнопка Report","Считает бюджет ошибки и NEA, рисует фигуры на вкладках Report."],
      ["Кнопка Check","Сухая проверка композиции без прогона конвейера: собирает и разрешает систему, показывает pass/fail с причиной (гварды геометрии и смыва), при успехе — f₁ и Q."],
      ["Кнопка Cancel","Кооперативно отменяет текущий расчёт (поздний результат отбрасывается)."],
      ["Кнопка Export","Сохраняет результаты (npz) и фигуры (PNG/PDF); вместе с ними — провенанс цепочки ЦОС (§3.7)."],
    ],
    [2700,6660]
  ));
  K.push(H(3,"Конструктор возбуждения и переключатели стадий","s_ui_controls"));
  K.push(para([R("Конструктор возбуждения ("), flink("src/optivibe/gui/widgets/excitation_builder.py","gui/widgets/excitation_builder.py"), R(") задаёт тип входного сигнала (синус — при желании с АМ/ЧМ-модуляцией, мультитон, свип, случайный по PSD, удар, композит из нескольких компонент) или импорт файла (CSV/WAV/TDMS/UFF/MAT/HDF5). Переключатели стадий ("), flink("src/optivibe/gui/widgets/control_panel.py","gui/widgets/control_panel.py"), R(") наглядно показывают слои физики — их можно включать по отдельности:")]));
  K.push(tbl(
    ["Стадия","Ключи (выбор)","Умолчание","Что меняет"],
    [
      ["Механика","modal / modal_time / stub","modal","Частотный или временной решатель H_lat(f); stub — тождество для регресса."],
      ["Оптика","cylinder / stub","cylinder","Физическая модель η(Δx) с цилиндром или линейный стаб."],
      ["Детектор","stub / photodiode","stub","photodiode добавляет фототок, шумы (дробовой/RIN/Джонсон), опорный канал, АЦП."],
      ["DSP","stub / standard","stub","standard — калиброванное восстановление a→v→x, спектры, ISO, NEA."],
      ["Чувствительность","static / operating_point / nonlinear_curve","static","Модель s_target: статическая, по рабочей точке, нелинейная (для >50g)."],
      ["Интегратор","frequency / time / leaky","frequency","a→v→x: спектральное 1/(jω), временное с детрендом или причинная схема потока над целой записью."],
    ],
    [1750,2750,1360,3500]
  ));
  K.push(para([R("Два последних комбо — "), R("зеркала", { bold: true }), R(" панели «DSP experiment» (§"), xr("3.7","s_cmp"), R("): одно и то же значение модели, показанное в двух местах, а не две копии. Владельцем значений остаётся панель эксперимента, поэтому смена там и здесь даёт один и тот же прогон.")]));
  K.push(para([R("Замечание: детектор и DSP по умолчанию — "), c("stub"), R(" (идеальные), чтобы реалистичный шум не «прятал» демонстрационные эффекты; для восстановления вибрации включите "), c("photodiode"), R(" и "), c("standard"), R(" (как в "), flink("examples/recover_sine.yaml"), R(").")]));
  K.push(H(3,"Язык, тема, справка и настройки","s_ui_prefs"));
  K.push(para([R("Интерфейс двуязычный: русский и английский переключаются "), R("вживую", { bold: true }), R(", без перезапуска (каталог "), flink("src/optivibe/gui/i18n.py","gui/i18n.py"), R("). Переключение пересобирает панель управления из её же полезной нагрузки, поэтому набранные значения переживают смену языка. Осознанные исключения: чистая математика подписей осей и английские идентификаторы вариантов/метрик внутри заголовков не переводятся, а легенды живых графиков обновятся на следующем прогоне.")]));
  K.push(para([R("Ядро и "), c("viz"), R(" остаются языко-независимыми — подписи фигур matplotlib переводятся на стороне GUI при отрисовке. Это не косметическое решение: физический слой не должен зависеть от языка интерфейса.")]));
  K.push(tbl(
    ["Где","Что настраивается"],
    [
      ["Диалог «Настройки» (Ctrl+,)","Язык, тема оформления (светлая/тёмная), восстановление геометрии окна. Язык и тема применяются немедленно, всё запоминается между запусками."],
      ["Строка меню","File (Export, Quit) · Run (Run Ctrl+R, Report Ctrl+Shift+R, Check Ctrl+K, Cancel Esc) · View (Настройки, панель журнала) · Help (What's-This Shift+F1, руководство, о программе)."],
      ["Знак «?» у поля","Короткая справка «что это и зачем» у каждого редактируемого параметра и заголовок-описание у каждой вкладки. Тот же текст показывает режим What's-This."],
      ["Панель журнала","Док-панель, зеркалящая журнал приложения; включается из меню View."],
    ],
    [2700,6660]
  ));
  K.push(para([R("Мелочь, экономящая нервы: колесо мыши над выпадающим списком или числовым полем "), R("не меняет значение", { bold: true }), R(", а прокручивает панель — прокрутка длинной формы больше не переставляет параметры незаметно.")]));

  K.push(H(3,"Модель потоков: расчёт вне UI","s_ui_threads"));
  K.push(para([R("Тяжёлые прогоны выполняются в рабочем потоке, поэтому окно не «зависает». Qt-free задачи ("), flink("src/optivibe/gui/workers/jobs.py","gui/workers/jobs.py"), R(": "), c("ScenarioJob"), R(", "), c("ReportJob"), R(", "), c("SweepJob"), R(", "), c("MonteCarloJob"), R(", "), c("CompareJob"), R(") вызывают только "), c("pipeline"), R("/"), c("analysis"), R(" и исполняются общим "), c("JobWorker(QObject)"), R(" на "), c("QThread"), R(" под управлением "), c("JobController"), R(" ("), flink("src/optivibe/gui/controllers/job_controller.py","gui/controllers/job_controller.py"), R("). Прогресс/результат/ошибка приходят сигналами; отмена кооперативная. Инвариант «расчёт идёт вне UI-потока» закреплён тестом "), c("test_computation_runs_off_the_gui_thread"), R(".")]));
  K.push(para([R("У живого режима — "), R("второе, отдельное", { bold: true }), R(" семейство воркеров ("), flink("src/optivibe/gui/workers/stream.py","gui/workers/stream.py"), R("): разовая задача по смыслу выдаёт ровно один результат, а поток эмитит кадры до команды «стоп». Контракт разовых задач ради этого не расширялся; вместо этого поток получил свой воркер и свой контроллер, а взаимное исключение живого режима и разовых прогонов сделано явной политикой окна (§"), xr("3.6","s_live"), R(").")]));
  K.push(links([ xr("Реализация воркера → §7.10","s_impl_gui"), sep(), xr("Архитектура GUI → §5","ch5"), sep(), xr("Живой режим → §3.6","s_live"), sep(), xr("Стенд сравнения → §3.7","s_cmp"), sep(), flink("src/optivibe/gui/"), sep(), R("база знаний: документ 09 §9",{size:18,color:MUT}) ]));

  return K;
}

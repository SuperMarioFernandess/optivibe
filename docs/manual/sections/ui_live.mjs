// docs/manual/sections/ui_live.mjs
// Глава 3.6 — живой режим (осциллограф) поверх причинного потокового слоя
// (S-03/H-3, SW-68; O-SW-03, SW-72). Свёрнуто по коду dsp/streaming.py,
// gui/workers/stream.py, gui/widgets/live_controls.py, gui/widgets/live_view.py.
// Owns its bookmarks/links. Edit this module only; build.mjs orders it via manifest.mjs.
import {
  R, c, P, para, H, xr, flink, ext, links, sep, code, cap, bullets, nums, img, tbl, fml,
  AlignmentType, Paragraph, TextRun, PageBreak,
  REPO, FIG, INK, MUT, ACC, ACCD, LINE, CW,
} from "../shared.mjs";

export function section() {
  const K = [];
  K.push(H(2, "3.6 Живой режим: непрерывный поток на вкладке Live", "s_live"));
  K.push(para([R("Обычный прогон конечен: сценарий считается целиком, и только потом появляются графики. Живой режим — второй способ работы той же вкладки Live: источник отдаёт сигнал "), R("кадрами", { bold: true }), R(", а обработка идёт непрерывно, как у настоящего прибора. Кнопка «Start live» на панели живого режима ("), flink("src/optivibe/gui/widgets/live_controls.py", "gui/widgets/live_controls.py"), R(") запускает поток; пока он идёт, разовые прогоны заблокированы, и наоборот — это явная политика окна, а не побочный эффект, и нужна она ради однозначности происхождения чисел на экране.")]));

  // ---- why causal ----
  K.push(H(3, "Почему поток считается иначе, чем батч", "s_live_causal"));
  K.push(para([R("Батчевые интеграторы "), R("непричинны", { bold: true }), R(": частотный маршрут берёт полноспектральное преобразование всей записи, временной — детренд по всей записи и фильтр с нулевой фазой. Ни один из них не может выдать ни одного отсчёта, пока запись не известна целиком. Поэтому реальное время обслуживает отдельный "), R("причинный", { bold: true }), R(" слой ("), flink("src/optivibe/dsp/streaming.py", "dsp/streaming.py"), R("): leaky-интегратор 1-го порядка, несущий состояние фильтра между кадрами, кольцевой буфер и экспоненциально усредняемый спектр.")]));
  K.push(para([R("Отсюда важное следствие, которое стоит понять до чтения чисел: бит-в-бит совпадения потока и батча на стадии интеграторов "), R("не бывает по конструкции", { bold: true }), R(". Приёмка потока формулируется иначе и именно так и проверена: калибровка совпадает с батчем бит-в-бит; результат не зависит от того, какими кадрами нарезан сигнал (швонезависимость, бит-в-бит); метрики в полосе сходятся с батчем и с аналитикой в пределах допусков — "), R("после прогрева", { bold: true }), R(". Вывод — theory-06 §3.6/§7.6 ("), flink("docs/theory/06_dsp_algorithm.md", "docs/theory/06_dsp_algorithm.md"), R("); здесь он не пересказывается.")]));

  // ---- five blocks: warmed ----
  K.push(H(3, "Индикатор warmed (прогрев)", "s_live_warmed"));
  K.push(...nums([
    [R("Что это в UI. ", { bold: true }), R("Первый элемент строки провенанса под графиками: «прогрев» либо «прогрет». Меняется сам, без участия пользователя.")],
    [R("Что это физически / зачем. ", { bold: true }), R("Причинные фильтры стартуют из нулевого состояния, и двойное интегрирование даёт переходный процесс. Пока он не осел, СКЗ и особенно перемещение — "), R("не окончательные числа", { bold: true }), R(". Индикатор — не украшение: он отделяет «прибор ещё думает» от «прибор показывает».")],
    [R("Как устроено внутри. ", { bold: true }), R("Флаг приходит "), R("из расчётного слоя", { italics: true }), R(" ("), c("StreamingDsp"), R(" → поле "), c("warmed"), R(" кадра "), c("StreamFrame"), R("), а не вычисляется видом. GUI ничего не считает: он нарезает блоки и решает, "), R("когда", { italics: true }), R(" отдать кадр.")],
    [R("Что меняет (цепочка следствий). ", { bold: true }), R("Значения до прогрева нельзя ни экспортировать как результат, ни сравнивать с предсказанием: расхождение будет измерять переходный процесс, а не физику.")],
    [R("Связи и типичные ошибки. ", { bold: true }), R("Типичная ошибка — «нажал старт, сфотографировал экран». Дайте потоку прогреться; длина окна показана рядом, чтобы полсекунды следа не принимали за всю запись.")],
  ], "n4"));

  // ---- controls ----
  K.push(H(3, "Управление потоком", "s_live_controls"));
  K.push(tbl(
    ["Элемент", "Значения", "Что делает"],
    [
      ["Start / Stop live", "—", "Запускает и кооперативно останавливает поток. Пока поток идёт, Run/Report/Sweep недоступны (и обратно)."],
      ["Источник", "синтетический сценарий | запись прибора", "Сценарий — прямая цепочка модели, собранная слева; запись — тот же дескриптор kind: analyze, что и у CLI (config-first, headless-паритет)."],
      ["Темп", "реальное время | максимально быстро", "Реальный темп следует часам записи; ускоренный гонит кадры без обязательств перед часами."],
      ["Один проход / цикл", "—", "Конечное проигрывание длительности сценария либо зацикливание со счётчиком оборотов и пометкой стыка."],
      ["Частота обновления", "кадров в секунду", "Эталонное значение — 10/с: плавно для человека и не выше темпа, с которым спектральное окно вообще обновляется."],
    ],
    [1900, 2600, 4860]
  ));
  K.push(para([R("Пропуски отсчётов возможны только там, где есть часы, которые можно пропустить. В реальном темпе цикл меряет отставание и при отставании больше блока перескакивает вперёд, отмечая пропуск, — «выбрасываем старое, показываем свежее», как и полагается живому монитору. В ускоренном режиме обязательств перед часами нет, пропуск невозможен по построению, и счётчик показывает "), R("«н/д», а не «0»", { bold: true }), R(": успокаивающий ноль на месте «неприменимо» — такая же неправда, как декоративный индикатор.")]));

  // ---- honest gaps ----
  K.push(H(3, "Что в живом режиме пусто — и почему это правильно", "s_live_gaps"));
  K.push(para([R("Две панели, привычные по разовому прогону, в потоке остаются пустыми с подписью. Это принятое решение, а не незавершённая работа:")]));
  K.push(...bullets([
    [R("Кривая входа («истина против восстановления»). ", { bold: true }), R("У потока нет истины, выровненной по окну отображения: окно бежит, а эталонный сигнал пришлось бы синхронно нарезать и хранить отдельно. Показать «почти совпадающую» кривую было бы хуже, чем не показать ничего.")],
    [R("Панель сигнала детектора. ", { bold: true }), R("Снимок кадра несёт "), R("восстановленные", { italics: true }), R(" сигналы, а не сырьё; дублировать сырой фототок в представлении значило бы завести второй кольцевой буфер в виде — ровно там, где инвариант требует, чтобы вид ничего не хранил и не считал.")],
    [R("Стык при зацикливании. ", { bold: true }), R("Бесконечный синтетический стимул сейчас получается зацикливанием конечной записи, и на склейке возникает настоящий разрыв — широкополосный всплеск. Он "), R("помечается", { bold: true }), R(" (в строке провенанса), а не сглаживается: инструмент, созданный отличать физику от артефактов, не должен впрыскивать рукотворный артефакт молча. Честная фазовая непрерывность запланирована отдельной задачей.")],
  ]));
  K.push(para([R("Маркеры ожидаемых пиков (§"), xr("6", "ch6"), R(") в живом режиме работают: оверлей собирается из конфигурации потока — это арифметика по конфигу, а не расчёт по данным. У записи, у которой нет сценария, оверлея не будет — вместо угаданного.")]));
  K.push(links([xr("Разовый прогон и вкладки → §3.1", "s_ui_gui"), sep(), xr("Стенд сравнения ЦОС → §3.7", "s_cmp"), sep(), flink("src/optivibe/dsp/streaming.py"), sep(), flink("src/optivibe/gui/workers/stream.py"), sep(), R("вывод: theory-06 §3.6/§5/§7.6", { size: 18, color: MUT })]));

  return K;
}

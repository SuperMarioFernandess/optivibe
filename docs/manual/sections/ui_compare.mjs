// docs/manual/sections/ui_compare.mjs
// Глава 3.7 — стенд сравнения цепочки ЦОС (S-22 W-1/W-2/W-3, SW-73). Свёрнуто по
// коду analysis/compare.py, gui/widgets/dsp_controls.py, gui/widgets/compare_panel.py,
// cli/main.py. Owns its bookmarks/links; build.mjs orders it via manifest.mjs.
import {
  R, c, P, para, H, xr, flink, ext, links, sep, code, cap, bullets, nums, img, tbl, fml,
  AlignmentType, Paragraph, TextRun, PageBreak,
  REPO, FIG, INK, MUT, ACC, ACCD, LINE, CW,
} from "../shared.mjs";

export function section() {
  const K = [];
  K.push(H(2, "3.7 Стенд сравнения цепочки ЦОС", "s_cmp"));
  K.push(para([R("Обратная цепочка — не единственно возможная: интегрировать можно спектрально или во времени, спектр оценивать одиночным преобразованием или методом Уэлча, окно взять то или это. Стенд отвечает на один вопрос: "), R("что меняется в числах, когда меняешь ступень", { bold: true }), R(" — и отвечает на одном и том же входе, чтобы разница не могла прийти откуда-то ещё. Две части: панель «Эксперимент: цепочка ЦОС» слева (выбор ступеней) и вкладка «Сравнение» вверху (две цепочки бок о бок).")]));

  // ---- five blocks: the verdict badge ----
  K.push(H(3, "Бейдж verified / experimental", "s_cmp_verdict"));
  K.push(...nums([
    [R("Что это в UI. ", { bold: true }), R("Плашка вверху панели эксперимента: «verified» на дефолтной цепочке, «experimental» — как только хоть одна ручка отклонилась, со списком отклонений. Рядом — кнопка «вернуть дефолт».")],
    [R("Что это физически / зачем. ", { bold: true }), R("Весь аппарат проверки — golden-набор, планы тестирования и V&V, бит-идентичность потока и батча — покрывает "), R("дефолтную цепочку и только её", { bold: true }), R(". Прогон с другими ступенями остаётся законным экспериментом, но его числа не являются выходом верифицированного двойника, и путать одно с другим нельзя.")],
    [R("Как устроено внутри. ", { bold: true }), R("Вердикт "), R("вычисляется", { bold: true }), R(" из значений опций ("), c("chain_status"), R(" в "), flink("src/optivibe/analysis/compare.py", "analysis/compare.py"), R("), сравнением с эталонной цепочкой. Объявить цепочку верифицированной из интерфейса нельзя — такого пути в API нет.")],
    [R("Что меняет (цепочка следствий). ", { bold: true }), R("Вердикт и полный список отклонений едут "), R("вместе с экспортом", { bold: true }), R(": в файл результатов добавлены ключи статуса и провенанса, рядом пишется "), c("run_provenance.yaml"), R(" с версией и ревизией кода. Выгруженные экспериментальные числа опознаются по файлу, а не по памяти оператора.")],
    [R("Связи и типичные ошибки. ", { bold: true }), R("Ошибка — «покрутил ручки, получил красивее, отправил в отчёт». Красивее ≠ вернее: см. "), xr("§8 Ворота и приёмка", "s_test_gates"), R(".")],
  ], "n5"));

  // ---- applicability ----
  K.push(H(3, "Тег применимости: батч / поток / оба", "s_cmp_applicability"));
  K.push(para([R("У каждой строки панели стоит пометка, до какого расчётного пути ручка вообще доходит. Она берётся из карты "), c("CHAIN_APPLICABILITY"), R(" в ядре, а не выдумывается виджетом, и полнота карты закреплена тестом: новое поле модели не может появиться без тега. Смысл пометки не бюрократический — «батч-только» здесь следствие причинности, а не недоделка: цепочка реального времени не знает записи целиком, поэтому интегратора с нулевой фазой, преобразования по всей длине записи и её сегментации по Уэлчу там "), R("просто не существует", { bold: true }), R(".")]));
  K.push(tbl(
    ["Ручка", "Путь", "Что выбирается"],
    [
      ["integrator", "батч", "frequency (спектральный 1/(jω)) · time (интегрирование с детрендом) · leaky (причинная схема потока, применённая к целой записи — видно, чего стоит причинность)"],
      ["spectrum_method", "батч", "одиночное преобразование либо PSD по Уэлчу"],
      ["window", "оба", "шесть оконных функций; у каждой — свой аналитический репер (амплитудная коррекция и Парсеваль)"],
      ["welch_nperseg / welch_noverlap", "батч", "длина сегмента и перекрытие оценки Уэлча"],
      ["f_hp_hz", "оба", "срез ФВЧ интеграторов (убирает дрейф двойного интегрирования)"],
      ["f_c_stream", "поток", "причинный срез leaky-интегратора; намеренно независим от батчевого f_hp (theory-06 §9.3-2)"],
      ["sensitivity_model / sensitivity_freq", "оба / батч", "как сквозная чувствительность привязана к рабочей точке и к частоте"],
      ["peak_interpolation", "оба", "суб-бинная интерполяция доминанты; выключенная показывает сетку бинов, на которой оценка работает на самом деле"],
      ["iso_machine_class", "оба", "класс машины для оценки по ISO"],
    ],
    [2100, 1100, 6160]
  ));
  K.push(para([R("Осознанное исключение: калибровка на панель не выведена (это территория отдельной задачи по стендовой калибровке), и не выведена вторая ручка деконволюции — тот же эффект уже управляется моделью чувствительности, а два органа управления на один эффект были бы ловушкой, а не уроком. Отдельно назовём "), R("известную асимметрию", { bold: true }), R(": окно доходит до PSD (а через неё до полосного СКЗ и оценки ISO) и до потокового спектра, но не до амплитудного спектра при одиночном преобразовании. Она названа прямо в подсказке к полю и вынесена как открытый вопрос — «тихо пробросить» нельзя, это сдвинуло бы доминанты приёмок и все эталонные тесты.")]));

  // ---- compare tab ----
  K.push(H(3, "Вкладка «Сравнение»", "s_cmp_tab"));
  K.push(para([R("Вкладка берёт "), R("один", { bold: true }), R(" вход и прогоняет по нему две цепочки: наложение восстановленного ускорения и спектра плюс таблицу расхождений по метрикам (СКЗ ускорения/скорости/перемещения, доминанта и их число, коэффициент второй гармоники, полосное СКЗ скорости) — с абсолютной и относительной разницей к цепочке-эталону. Вердикт каждой цепочки виден тут же. Расчёт идёт в рабочем потоке, окно не блокируется.")]));
  K.push(para([R("Ключевая гарантия: вход открывается "), R("теми же швами", { bold: true }), R(", что у обычного анализа записи и у живого режима — сценарий через прямую цепочку, запись через штатное чтение с калибровкой, объявленной в её спеке. Это закреплено двумя эталонными тестами, а не абзацем: иначе разница в числах могла бы прийти от входа, то есть стенд опровергал бы сам себя. Физики в стенде тоже нет — каждая цепочка исполняется неизменённой стандартной реализацией ЦОС, стенд лишь подаёт ей опции и сводит метрики в таблицу.")]));

  // ---- CLI ----
  K.push(H(3, "То же самое из командной строки", "s_cmp_cli"));
  K.push(para([R("Эксперимент, существующий только как состояние виджета, невоспроизводим. Поэтому цепочка — это конфигурация: спека "), c("kind: compare"), R(" по образцу спеки анализа, а GUI её лишь редактирует.")]));
  K.push(...code([
    "# examples/compare_integrators.yaml",
    "kind: compare",
    "name: integrators-and-spectra",
    "source:",
    "  scenario: examples/recover_sine.yaml   # либо record: <спека kind: analyze>",
    "chains:",
    "  - name: default            # без блока dsp — это и есть верифицированная цепочка",
    "  - name: time-integrator",
    "    dsp: { integrator: time }",
    "  - name: welch-spectrum",
    "    dsp: { spectrum_method: welch, welch_nperseg: 4096 }",
    "",
    "optivibe compare examples/compare_integrators.yaml \\",
    "        --provenance out/compare.provenance.yaml",
  ]));
  K.push(para([R("Первая цепочка — эталон таблицы; если сделать ею дефолтную, чтение таблицы сверху вниз прямо отвечает на вопрос «во что обходится уход с верифицированного пути». Экспорт сравнения — файл результатов на цепочку, текстовая таблица расхождений и провенанс.")]));
  K.push(links([xr("Живой режим → §3.6", "s_live"), sep(), xr("CLI → §3.2", "s_ui_cli"), sep(), xr("Алгоритмы ЦОС → §6.5", "s_algo_dsp"), sep(), flink("src/optivibe/analysis/compare.py"), sep(), flink("src/optivibe/gui/widgets/dsp_controls.py"), sep(), R("база знаний: 17 §1/§7, 19 §2", { size: 18, color: MUT })]));

  return K;
}

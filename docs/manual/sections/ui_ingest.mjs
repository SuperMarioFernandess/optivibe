// docs/manual/sections/ui_ingest.mjs
// Глава 3.5 — ввод измеренных параметров (S-13; SW-63/SW-64). Свёрнуто по коду
// io/characterization.py, io/ingest.py, cli/main.py, gui/widgets/subsystem_forms.py.
// Owns its bookmarks/links. Edit this module only; build.mjs orders it via manifest.mjs.
import {
  R, c, P, para, H, xr, flink, ext, links, sep, code, cap, bullets, nums, img, tbl, fml,
  AlignmentType, Paragraph, TextRun, PageBreak,
  REPO, FIG, INK, MUT, ACC, ACCD, LINE, CW,
} from "../shared.mjs";

export function section() {
  const K = [];
  K.push(H(2, "3.5 Ввод измеренных параметров (характеризация прибора)", "s_ingest"));
  K.push(para([R("До сих пор все числа двойника приходили из конфигурации — пресетов и переопределений (§"), xr("3.4", "s_cfg_subsys"), R("). Этот раздел — про обратное направление: как "), R("измеренная", { bold: true }), R(" величина реального прототипа попадает в композицию так, чтобы вместе с ней ехали неопределённость, метод и происхождение. Слой рассчитан на фазу 0 стендового плана (характеризация прототипа, 20 §1): длина консоли, зазор, радиус наконечника, спектр источника, трасса RIN, ring-down добротности.")]));
  K.push(para([R("Важная оговорка о зрелости: реализован "), R("тракт", { bold: true }), R(", а не данные. Измерений фазы 0 пока нет, поэтому конфигурация прототипа "), c("configs/variants/proto_poc.yaml"), R(" по-прежнему содержит плейсхолдеры; снятие плейсхолдеров — и есть назначение этого слоя.")]));

  // ---- artifact = data + sidecar ----
  K.push(H(3, "Артефакт измерения: трасса плюс обязательный сайдкар", "s_ingest_artifact"));
  K.push(para([R("Одно измерение — это пара файлов: CSV-трасса с прибора и YAML-сайдкар рядом с ней, с тем же именем. Сайдкар — не метаданные «для порядка», а часть контракта: без объявленных единиц и неопределённости трасса не принимается вовсе (модель "), c("CharacterizationSpec"), R(" в "), flink("src/optivibe/io/characterization.py", "io/characterization.py"), R("). Для ручных величин (kind "), c("scalar"), R(") сайдкар и есть само измерение — файла данных нет.")]));
  K.push(...code([
    "# ringdown_2026-08-01.yaml — сайдкар рядом с ringdown_2026-08-01.csv",
    "kind: ringdown             # scalar | spectrum | rin_psd | ringdown | profile",
    "instrument: 'Tektronix MDO34 + PD, 2 МС/с'   # обязателен (провенанс)",
    "timestamp: '2026-08-01T11:20:00+03:00'       # обязателен (провенанс)",
    "method: 'удар пьезо-щелчком, свободное затухание в воздухе'",
    "data_file: ringdown_2026-08-01.csv",
    "columns:                   # единицы обязательны: незнакомая — громкий отказ",
    "  x: { name: time,  unit: s }",
    "  y: { name: amplitude, unit: V }",
    "uncertainties:             # объявленная u побеждает вычисленную",
    "  q_total: 45.0",
    "",
    "# ручная величина: сайдкар без файла данных",
    "kind: scalar",
    "parameter: length_m",
    "value: 2.013",
    "unit: mm                   # приводится к СИ по семейству единиц",
    "u: 0.004                   # в тех же единицах; без u параметр в конфиг не пойдёт",
  ]));
  K.push(cap("Сайдкар объявляет единицы, прибор, время и неопределённости; несовпадение — отказ, а не догадка."));

  // ---- five blocks: the GUM gate ----
  K.push(H(3, "Правило «нет u — нет входа в конфиг»", "s_ingest_gum"));
  K.push(...nums([
    [R("Что это в UI. ", { bold: true }), R("Поле "), c("u"), R(" сайдкара и колонка «неопределённость» в отчёте "), c("ingest"), R(". Параметр без "), c("u"), R(" печатается в отчёте как "), R("informational", { italics: true }), R(" и в композицию не записывается.")],
    [R("Что это физически / зачем. ", { bold: true }), R("Правило GUM (17 §4): число без неопределённости нельзя ни сравнить с предсказанием модели, ни включить в бюджет. Стендовая сверка идёт по критерию |X_модель − X_стенд| ≤ 2·√(u_м² + u_с²) (19 §3.3) — без "), c("u"), R(" критерий не определён, и «измерение» превратилось бы в аккуратно оформленное мнение.")],
    [R("Как устроено внутри. ", { bold: true }), R("Контракт "), c("MeasuredParameter"), R(" (frozen pydantic: имя конфиг-поля, значение в СИ, "), c("u ≥ 0 | None"), R(", метод) — правило закодировано в "), c("apply_measurements"), R(" ("), flink("src/optivibe/io/ingest.py", "io/ingest.py"), R("), а не оставлено на дисциплину оператора. Для "), c("kind: scalar"), R(" отсутствие "), c("u"), R(" отвергается ещё валидатором сайдкара.")],
    [R("Что меняет (цепочка следствий). ", { bold: true }), R("Принятое измерение → override подсистемы → тот же путь резолва, что у ручного редактирования формы; новых путей резолва слой не создаёт, поэтому эталон A/B/C/D по построению не задет.")],
    [R("Связи и типичные ошибки. ", { bold: true }), R("Типичная ошибка — «померили и вписали в YAML руками»: значение попадёт в конфиг, а провенанс и "), c("u"), R(" — нет, и на сверке L3 сравнивать будет нечего. См. "), xr("§3.4 Композиция", "s_cfg_subsys"), R(" и "), xr("§8 Ворота", "s_test_gates"), R(".")],
  ], "n3"));

  // ---- kinds ----
  K.push(H(3, "Пять видов артефактов", "s_ingest_kinds"));
  K.push(para([R("Виды зарегистрированы в реестре "), c("CHARACTERIZATION_REGISTRY"), R(" — тем же приёмом, что загрузчики записей: новый вид артефакта или формат файла добавляется регистрацией, а не правкой ядра (§"), xr("5.4", "s_arch_registry"), R("). Каждый ридер выполняет цепочку «загрузить → проверить → свернуть → выдать (параметры, u, провенанс)».")]));
  K.push(tbl(
    ["Вид", "Что это за трасса", "Во что сворачивается", "Проверки"],
    [
      ["scalar", "ручная величина (L, A, ρ, CMRR, DOP…); файла данных нет", "значение в СИ по семейству единиц", "обязательные value/unit/u; лишний data_file — отказ"],
      ["spectrum", "спектр источника с OSA: (λ, S_λ)", "центроид λ₀, численная FWHM Δλ, шумоэквивалентная Δν_eff, пол RIN = 2τ_c; таблица сохраняется для lineshape: measured", "окно центроида 0.1–20 мкм (ловушка «нм вместо м»); квадратуры — те же, что в optics/source.py"],
      ["rin_psd", "RIN(f) со стенда PD+TIA, уже с вычтенным полом", "медианный rin_db_hz в объявленной полосе band", "полоса обязательна; замещает выведенный ASE-пол, не складывается с ним"],
      ["ringdown", "свободное затухание после удара: (t, огибающая)", "σ из линейной подгонки ln(env) → Q = π·f₁/σ; u(Q) из ковариации фита ⊕ u(f₁)", "равномерная сетка t, ≥10 циклов, σ > 3·u_σ"],
      ["profile", "профиль наконечника по одному азимуту: (x, z)", "R_c круговой подгонкой Kåsa; u(R_c) из ковариации ЛСК", "≥20 точек, RMS-остаток ≤5 % R_c, коллинеарность — отказ"],
    ],
    [1200, 2400, 3260, 2500]
  ));
  K.push(para([R("Астигматизм наконечника (два азимута) в v1 не сворачивается — один азимут даёт один R_c; это отмеченный планами остаток, а не молчаливое упрощение.")]));

  // ---- anti-double-count ----
  K.push(H(3, "Анти-даблкаунт: измеренное замещает, а не добавляется", "s_ingest_double"));
  K.push(para([R("Самое опасное место слоя — не единицы, а "), R("двойной учёт", { bold: true }), R(". Многие величины двойник умеет "), R("выводить", { italics: true }), R(": пол RIN — из ширины линии, добротность — из модели Q(L). Если измеренное просто «добавить», один и тот же физический эффект войдёт в бюджет дважды. Поэтому правило в коде однозначно: измеренная величина "), R("замещает", { bold: true }), R(" свой модельный аналог.")]));
  K.push(...bullets([
    [R("Измеренная трасса RIN "), R("замещает", { bold: true }), R(" выведенный ASE-пол: она уже содержит и его, и избыточный шум драйвера.")],
    [R("При измеренном спектре скалярная "), c("linewidth_fwhm_m"), R(" рядом с ним — ошибка (два источника истины на одну строку), поэтому принудительно обнуляется; а при отсутствии измеренного RIN обнуляется и «паспортный» "), c("rin_db_hz"), R(" пресета — пол выводится из измеренной таблицы.")],
    [R("Измеренный "), c("q_total"), R(" записывается как явный override вычислимой модели Q(L); предсказание модели при этом попадает "), R("в отчёт", { italics: true }), R(" — это и есть заготовленный кросс-чек уровня L3.")],
    [R("Два измерения одного и того же поля — конфликт и громкий отказ, а не «последнее выигрывает».")],
  ]));
  K.push(para([R("Отдельные величины принимаются как измерения, но конфиг-слота не имеют и идут только в отчёт: "), c("f1_hz"), R(" (это метрика валидации, а не вход модели) и "), c("dop"), R(" (степень поляризации намеренно не введена в модель — иначе пол RIN понижался бы «по предположению»). Отчёт честно помечает их как "), R("still-model-default", { italics: true }), R(".")]));

  // ---- CLI ----
  K.push(H(3, "Команда optivibe ingest", "s_ingest_cli"));
  K.push(...code([
    "optivibe ingest <sidecar.yaml> [<sidecar.yaml> ...] \\",
    "        [--base proto_poc|<path.yaml>] [--out <composition.yaml>] [--dry-run]",
    "",
    "# сухой прогон: печатает отчёт, ничего не пишет",
    "optivibe ingest bench/*.yaml --base proto_poc --dry-run",
  ]));
  K.push(para([R("Команда загружает базовую композицию, применяет измерения как переопределения подсистем, "), R("разрешает", { italics: true }), R(" результат — чтобы прогнать гварды композиции на измеренном двойнике (смыв интерференции, геометрия, Q(L)) — и печатает отчёт в трёх группах: что записано в конфиг, что осталось информационным, что по-прежнему берётся из модели. Без "), c("--out"), R(" композиция пишется в "), c("configs/user/systems/<база>_measured.yaml"), R(".")]));
  K.push(para([R("Провенанс сохраняется "), R("отдельным артефактом", { bold: true }), R(" — "), c("<out>.provenance.yaml"), R(" рядом с композицией, а не внутри неё. Решение осознанное: вариантный YAML остаётся чистым описанием системы, а бит-сравниваемый эталон A/B/C/D не обрастает зависимостью от файлов измерений. В провенансе — вид артефакта, путь сайдкара и файла данных, sha256, прибор и время.")]));

  // ---- GUI ----
  K.push(H(3, "Тот же слой из GUI", "s_ingest_gui"));
  K.push(para([R("У каждого вида артефакта есть точка входа на своей вкладке панели параметров: спектр и трасса RIN — на «Source», профиль наконечника — на «Reflector», ring-down — на «System» ("), flink("src/optivibe/gui/widgets/subsystem_forms.py", "gui/widgets/subsystem_forms.py"), R("). Диалог принимает как сайдкар, так и его CSV — второй файл находится по конвенции одинакового имени. Загрузка "), R("засевает поля формы", { bold: true }), R(" и показывает провенанс, но полноценный измеренный двойник с персистентным артефактом делается через "), c("optivibe ingest"), R(": так измерение остаётся воспроизводимым, а не состоянием виджета.")]));
  K.push(links([xr("Композиция подсистем → §3.4", "s_cfg_subsys"), sep(), xr("Импорт данных → §3.3", "s_ui_data"), sep(), flink("src/optivibe/io/characterization.py"), sep(), flink("src/optivibe/io/ingest.py"), sep(), R("база знаний: 16 §2a, 17 §4, 20 §1", { size: 18, color: MUT })]));

  return K;
}

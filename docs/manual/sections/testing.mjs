// docs/manual/sections/testing.mjs
// AUTO-SLICED (S-DOC-2) from manual_build_doc.js lines 645..680; content verbatim.
// Owns its bookmarks/links. Edit this module only; build.mjs orders it via manifest.mjs.
import {
  R, c, P, para, H, xr, flink, ext, links, sep, code, cap, bullets, nums, img, tbl, fml,
  AlignmentType, Paragraph, TextRun, PageBreak,
  REPO, FIG, INK, MUT, ACC, ACCD, LINE, CW,
} from "../shared.mjs";

export function section() {
  const K = [];
  K.push(H(1,"8. Запуск и тестирование","ch8"));
  K.push(para("Раздел для освоения и обучения других: как проверить, что сборка исправна, и как устроена стратегия тестов, доказывающая согласованность кода с физикой."));
  K.push(H(2,"8.1 Команды-ворота (DoD)","s_test_gates"));
  K.push(...code([
    "uv sync --locked --python 3.11 --extra dev --extra gui --extra io-formats",
    "ruff check . && ruff format --check .           # линт + формат → 152 файла",
    "mypy                                            # строгая типизация → 105 файлов, 0 ошибок",
    "QT_QPA_PLATFORM=offscreen pytest --cov=optivibe --cov-fail-under=85",
    "#  → 717 passed, покрытие ядра 92.33 % (gui/ исключён из порога)",
    "QT_QPA_PLATFORM=offscreen pytest -m golden      # подмножество golden → 159",
    "sha256sum tests/data/resolved_variants_golden.json   # контракт вариантов A/B/C/D",
    "optivibe run examples/hello.yaml                # dominant 120.000 Hz, exit 0",
  ]));
  K.push(para([R("Числа выше сняты фактическим прогоном на текущем "), c("main"), R(" — сверяйте с ними своё окружение. Пин Python "), R("3.11 обязателен", { bold: true }), R(": свежий numpy тянет типовые заглушки синтаксиса 3.12, ломающие проверку типов под 3.11; истина ворот — CI на 3.11.")]));
  K.push(para([R("Про extra "), c("gui"), R(". Охват проверки типов задан конфигом ("), c("files = [\"src\"]"), R(") и равен "), R("105 файлам в обоих окружениях", { bold: true }), R(" — и с установленным PySide6, и без него; от extras он не зависит. Деградирует "), R("строгость", { bold: true }), R(": без PySide6 виджеты вырождаются в нетипизированные объекты, и GUI-модули проверяются слабее, а не эквивалентно. Поэтому прежняя оговорка «ворота валидны только из окружения с "), c("gui"), R("» "), R("ослаблена в части типов, но не отменена", { bold: true }), R(" — без PySide6 набор тестов законно пропускает GUI-часть, и полный счётчик не собирается:")]));
  K.push(...code([
    "pytest -m 'not gui'    → 659 passed,  58 deselected   # без PySide6 это весь охват",
    "pytest -m gui          →  58 passed, 659 deselected   # pytest-qt, offscreen",
  ]));
  K.push(H(2,"8.2 Приёмочные сценарии","s_test_accept"));
  K.push(para("Набор сценариев демонстрирует и проверяет работу подсистем. Ожидаемые доминирующие частоты — стабильные реперы регресса."));
  K.push(tbl(
    ["Сценарий","Что демонстрирует","Ожидаемо"],
    [
      ["hello","базовый прогон (stub-оптика и stub-детектор)","120.000 Гц"],
      ["multitone / sweep / random / shock","генераторы возбуждения","120 / 14.5 / 597.5 / 24995 Гц"],
      ["replay_csv / replay_wav / replay_tdms","импорт реальных данных","50 / 440 / 50 Гц"],
      ["resonance_sweep","резонанс варианта D","5005 Гц (= f₁ D)"],
      ["cross_axis","перекрёстная ось y (квадратичный канал)","240 Гц (= 2f)"],
      ["linearity_ramp","нелинейность у границы линейности","≈ f₁ B"],
      ["noise_floor / sine_with_noise","детектор: пол шума и сигнал над ним","σ_i ≈ 2.3 нА / 200 Гц"],
      ["recover_sine","полное восстановление (1g восстановлен)","200 Гц, в пределах NEA"],
      ["vibration_severity","виброоценка по ISO","50 Гц, зона C"],
      ["reflector_sphere / plane / wedge","семейство форм отражателя (§4.7)","120 / 240 / 120 Гц"],
      ["proto_poc_2f","прототип у вершины η: подавленная 1f, доминанта на 2f","240 Гц (предсказание E1-P1)"],
      ["composite_modulated","композит и АМ/ЧМ: несущая, боковые, шумовая подложка","1000 Гц и боковые 3950 / 4050 Гц"],
      ["compare_integrators","стенд сравнения цепочек ЦОС (спека kind: compare)","default [verified] против двух experimental, §3.7"],
    ],
    [2350,4710,2300]
  ));
  K.push(para([R("Полный набор доминант — регресс-защита ворот: их неизменность проверяется при каждой сдаче кода, и сдвиг доминанты означает смену контракта, а не «уточнение».")]));
  K.push(H(2,"8.3 Стратегия тестов","s_test_strategy"));
  K.push(...bullets([
    [R("golden против аналитики: ",{bold:true}), R("сверка с реперами базы — f₁ ≈ 100/L², эффективный наклон оптики, NEA-полка и мастер-закон NEA ∝ L⁻⁴, s_target бит-в-бит для умолчаний v1. Правило жёсткое: golden ссылается на "), R("замкнутую формулу или стандарт", { bold: true }), R(", а не на «текущий выход кода» — иначе тест закрепляет ошибку вместо того, чтобы её ловить.")],
    [R("property-тесты (hypothesis): ",{bold:true}), R("Парсеваль для спектра, round-trip интегрирования a→x→a, линейность калибровки, детерминизм по сиду.")],
    [R("регресс и смоук: ",{bold:true}), R("импорт всех модулей, неизменность доминант и умолчаний; GUI-смоук (pytest-qt, offscreen) с доказательством расчёта вне UI-потока.")],
    [R("бит-идентичность как приёмка новых слоёв: ",{bold:true}), R("каждая волна доказывала, что не сдвинула прежнее поведение — «композит из одной компоненты ≡ эта компонента», «m = 0 и β_FM = 0 ≡ прежний тон», «окно следа None ≡ прежнее окно», «нетронутая панель эксперимента ≡ умолчания модели». Это сильнее, чем сравнение метрик: сравниваются массивы.")],
    [R("перекрёстная сверка вместо комбинаторики: ",{bold:true}), R("альтернативы цепочки ЦОС (три интегратора, шесть окон, интерполяция пика) проверяются не полным перебором сочетаний, а "), R("общим аналитическим репером", { bold: true }), R(" — v = A/ω, x = A/ω², амплитудная коррекция и Парсеваль для окон. Причинному маршруту репер применяется на устоявшемся хвосте: у него есть прогрев, и это та же физика, что публикуется как warmed.")],
    [R("эквивалентность поток↔батч: ",{bold:true}), R("калибровка и швы — бит-в-бит, метрики в полосе — в допусках после прогрева (§6.5-бис). Финальный кадр живого цикла побайтово равен драйверу реплея на той же записи.")],
  ]));
  K.push(links([ xr("Команды и приёмки → §3.2","s_ui_cli"), sep(), xr("Стенд сравнения → §3.7","s_cmp"), sep(), flink("tests/"), sep(), R("база знаний: документ 10 §10, 11 §7, 18 §5",{size:18,color:MUT}) ]));

  return K;
}

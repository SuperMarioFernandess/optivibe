// docs/manual/sections/algorithms.mjs
// AUTO-SLICED (S-DOC-2) from manual_build_doc.js lines 347..397; content verbatim.
// Owns its bookmarks/links. Edit this module only; build.mjs orders it via manifest.mjs.
import {
  R, c, P, para, H, xr, flink, ext, links, sep, code, cap, bullets, nums, img, tbl, fml,
  AlignmentType, Paragraph, TextRun, PageBreak,
  REPO, FIG, INK, MUT, ACC, ACCD, LINE, CW,
} from "../shared.mjs";

export function section() {
  const K = [];
  K.push(H(1,"6. Алгоритмы","ch6"));
  K.push(para("Глава описывает, что и как считает каждая стадия. Физическая основа — глава 4; блоки кода — глава 7; исходные файлы — по ссылкам."));

  K.push(H(2,"6.1 Возбуждение (генерация входа)","s_algo_exc"));
  K.push(para([R("Генераторы создают 3D-ускорение a(t) за контрактом "), c("Excitation"), R(": "), c("sine"), R(" (тон), "), c("multitone"), R(" (сумма тонов), "), c("sweep"), R(" (линейный/лог свип), "), c("random"), R(" (шум с заданной PSD/полосой — частотный синтез), "), c("shock"), R(" (полусинус), "), c("composite"), R(" (сумма нескольких компонент на одной сетке). Амплитуды в g переводятся в м/с² через g₀. Replay-источник читает файл загрузчиком (§3.3) и возвращает тот же контракт. Воспроизводимость — через "), c("numpy.random.Generator"), R(" с сидом сценария.")]));

  K.push(H(3,"Модуляция несущей: АМ и ЧМ","s_algo_mod"));
  K.push(para([R("У тона "), c("sine"), R(" есть опциональный блок модуляции — так двойник получает "), R("боковые линии", { bold: true }), R(", которых иначе в спектре не бывает. Формулы выведены в базе (11 §2.1.3), здесь — их смысл:")]));
  K.push(...bullets([
    [R("АМ: ", { bold: true }), R("a(t) = a_c·[1 + m·cos(2πf_m t + φ_m)]·sin(2πf_c t + φ_c). Несущая остаётся a_c, по бокам от неё на f_c ± f_m встают две линии высотой "), R("m·a_c/2", { bold: true }), R(" каждая. Средний квадрат — (a_c²/2)(1 + m²/2), пик — a_c(1+m).")],
    [R("Глубина m > 1 запрещена валидацией: ", { bold: true }), R("огибающая меняла бы знак, и m перестал бы быть отношением амплитуд. Выразительность при этом не теряется — ту же волну точно даёт композит из трёх тонов, и текст ошибки прямо называет этот путь.")],
    [R("ЧМ: ", { bold: true }), R("a(t) = a_c·sin(2πf_c t + β_FM·sin(2πf_m t + φ_m) + φ_c), где индекс β_FM = Δf/f_m. Линии стоят на f_c ± k·f_m с высотами a_c·|J_k(β_FM)| (функции Бесселя). Две особенности: несущая "), R("зависит от индекса", { bold: true }), R(" (a_c·|J₀|) и обращается в нуль при β_FM ≈ 2.4048; а полная мощность от индекса "), R("не зависит", { bold: true }), R(" — ⟨a²⟩ = a_c²/2 при любом β_FM, потому что Σ J_k² = 1.")],
  ]));
  K.push(para([R("Модулировать можно только "), c("sine"), R(": у свипа несущая плывёт, у удара её нет, у шума семейство боковых не определено. Несколько модулированных несущих собираются композитом.")]));

  K.push(H(3,"Композит: сумма компонент без перенормировки","s_algo_composite"));
  K.push(para([R("Композит складывает результаты тех же зарегистрированных генераторов — своей физики сигнала у него нет, поэтому композит из одной компоненты воспроизводит её "), R("бит-в-бит по построению", { bold: true }), R(", а не по совпадению. Каждая компонента может назвать свою ось; части суммируются поосевно, чем закрыто отложенное с S1 расширение «разные сигналы по осям». Файловый реплей компонентой быть не может (его частота и длина приходят из записи), вложенность запрещена, а сетка задаётся только композитом и инжектируется в компоненты — расхождение даёт громкую ошибку.")]));
  K.push(para([R("Уровень композита "), R("не перенормируется", { bold: true }), R(": каждая компонента несёт объявленный уровень. Альтернатива «привести композит к целевому уровню» отклонена по двум причинам, и обе стоит понимать. Во-первых, она несовместима с приёмкой «композит из одной компоненты ≡ эта компонента». Во-вторых, при «тон + шум» коэффициент считался бы по конкретной реализации шума — тогда "), R("смена сида меняла бы амплитуду тона", { bold: true }), R(", и высота линии перестала бы быть свойством конфигурации, обесценив весь протокол интерпретации спектра.")]));
  K.push(para([R("Проверяемость по Парсевалю формулируется честно, в двух частях: ⟨a²⟩ = ∫PSD df — всегда; СКЗ² = Σ СКЗ_i² — "), R("только при непересекающихся носителях", { bold: true }), R(" компонент (перекрёстные члены зануляются). Сумма может превысить полную шкалу варианта: это законное исследование (поведение за пределами FS — самостоятельный предмет), поэтому уровень "), R("сообщается, а не обрезается", { bold: true }), R(" — композит кладёт в метаданные суммарные СКЗ/пик и покомпонентные СКЗ, а оркестратор пишет предупреждение, когда пик выходит за FS.")]));
  K.push(para([R("Посев компонент: компонента 0 наследует сид сценария без изменений (иначе не выполнилась бы бит-идентичность для "), c("random"), R("), компоненты 1… берут детерминированный суб-сид. Без этого две "), c("random"), R("-компоненты получили бы "), R("один поток", { bold: true }), R(", и вместо суммы двух независимых шумов вышел бы один шум, умноженный на два, — тихая физическая ошибка. Дописывание компоненты в конец не двигает существующие потоки; вставка в середину пере-раскатывает последующие, поэтому у "), c("random"), R(" есть и собственный необязательный сид, пиннящий реализацию независимо от позиции.")]));
  K.push(links([ xr("Импорт данных → §3.3","s_ui_data"), sep(), xr("Ожидаемые пики → §6.8","s_algo_peaks"), sep(), xr("Реализация → §7.4","s_impl_exc"), sep(), flink("src/optivibe/excitation/"), sep(), R("база знаний: 11 §2.1, 01 §3.5",{size:18,color:MUT}) ]));

  K.push(H(2,"6.2 Механика: H_lat(f) и решатели","s_algo_mech"));
  K.push(para([R("По "), c("VariantConfig"), R(" (длина L и пр.) и константам вычисляются производные величины: f₁, H_lat^QS, коэффициент наклона 1.377/L, осевая податливость. Два решателя выбираются ключом реестра:")]));
  K.push(...bullets([
    [R("частотный (",{}), c("modal"), R(", умолчание): вход → rFFT → умножение на H(f) по осям → обратное FFT → "), c("TipState"), R(". Целевая ось: H_lat(f) = H_lat^QS·D(f); наклон θy = 1.377/L · dx; поперечная y — та же АЧХ (развязка осей делается оптикой); осевая z — квазистатическая податливость.")],
    [R("временной (",{}), c("modal_time"), R("): state-space осциллятор моды 1 — задел под нелинейности и удары; согласован с частотным на синусе.")],
  ]));
  K.push(para([R("Чистая функция формы моды "), c("first_mode_shape(xi, β₁L)"), R(" (φ₁(z/L), единичный торец) используется GUI для анимации изгиба; на конвейер не влияет.")]));
  K.push(links([ xr("Физика механики → §4.2","s_phys_mech"), sep(), xr("Реализация → §7.5","s_impl_mech"), sep(), flink("src/optivibe/mechanics/cantilever.py") ]));

  K.push(H(2,"6.3 Оптика: модель η","s_algo_opt"));
  K.push(para([R("Стадия "), c("cylinder"), R(" строит из варианта frozen-модель и вычисляет η из "), c("TipState"), R(" по замкнутым формулам (документ 03/04): η = η_x·η_y; эффективное смещение на отражателе dx_eff = Δx₀ + dx + (R_c+g)·θy, где g = A + dz. Зависимость от dx_eff — точная гауссиана, что даёт замкнутые η₀, наклон, эффективный наклон ×[1+1.377(R_c+A)/L] и анизотропию. Плоская плоскость цилиндра не зависит от dy (симметрия) — отсюда нулевой 1-й порядок по y. Валидаторы конфигурации требуют форму «cylinder», R_c ≥ 5·w₀ и w(A) ≤ R_c/3.")]));
  K.push(links([ xr("Физика оптики → §4.3","s_phys_opt"), sep(), xr("Реализация → §7.6","s_impl_opt"), sep(), flink("src/optivibe/optics/cylinder.py") ]));

  K.push(H(2,"6.4 Детектор: шумы и АЦП","s_algo_det"));
  K.push(para([R("Стадия "), c("photodiode"), R(" формирует фототок I = ℛP(R₁+ρη), "), c("dc_level"), R(" = ℛP(R₁+ρη₀), затем добавляет независимые шумы на полосе Найквиста: дробовой (√(2e·I_DC·fs/2)), RIN (уровень из конфига; для SLD ≈ −126 дБ/Гц), джонсоновский (через R_f). Балансный опорный канал подавляет RIN множителем 10^(−CMRR/10) ("), c("reference_arm∈{matched,bright}"), R("). АЦП: AC-связь (снят пьедестал), квантование до N бит на полной шкале, опц. antialias и насыщение с предупреждением. Субсид шума детерминирован сидом сценария.")]));
  K.push(links([ xr("Физика шумов/NEA → §4.4","s_phys_det"), sep(), xr("Реализация → §7.7","s_impl_det"), sep(), flink("src/optivibe/detector/photodiode.py") ]));

  K.push(H(2,"6.5 DSP / обратная задача","s_algo_dsp"));
  K.push(para([R("Стадия "), c("standard"), R(" восстанавливает вибрацию из отсчётов:")]));
  K.push(...nums([
    [R("калибровка: ",{bold:true}), R("снятие DC, деление I_AC на знаковую сквозную чувствительность s_target [А/(м/с²)] → ускорение a целевой оси;")],
    [R("кинематика a→v→x: ",{bold:true}), R("интегратор frequency (спектральное 1/(jω) с ВЧ-маской), time (cumtrapz + детренд/Баттерворт) или leaky (причинная схема потокового слоя, применённая к целой записи — учебная альтернатива, §6.5-бис); фаза контролируется (x отстаёт от a на π);")],
    [R("спектры: ",{bold:true}), R("амплитудный спектр (rFFT 2/N), PSD (Welch), спектрограмма; выделение доминирующих частот;")],
    [R("метрики: ",{bold:true}), R("СКЗ a/v/x; виброоценка по ISO 10816-3/20816-3 (СКЗ скорости в полосе); перекрёстный остаток (доля 2f);")],
    [R("NEA: ",{bold:true}), R("приведение шумовой СПМ из метаданных детектора ко входу делением на |s_target|²; полнополосный NEA·√B; сверка с аналитикой (≤15%). Ветвей ЧЕТЫРЕ: к трём токовым (дробовая, RIN, джонсоновская) квадратурно добавляется тепловой (броуновский) пол консоли NEA_th — он механический, поэтому складывается уже в домене ускорения, а не как СПМ тока. Ветвь включена по умолчанию (include_thermal=True); отключение бит-в-бит воспроизводит поведение до M-12. Следствие: вблизи f₁, где оптический NEA ныряет как 1/Q, полный спектр садится на тепловой пол.")],
  ],"n1"));
  K.push(para("Тонкость двойного интегрирования (PSD_x = PSD_a/ω⁴) отделяет малую ошибку восстановления a от повышенного пола перемещения x."));
  K.push(para([R("Важное исключение (защита от двойного учёта). "), R("При анализе РЕАЛЬНЫХ записей (роль «анализатор выхода прибора») тепловая ветвь "), R("намеренно не добавляется", { bold: true }), R(": измеренный фототок уже физически содержит броуновское движение консоли. Аналитический бюджет и измеренный спектр — разные источники, складывать их нельзя. Обратная сторона того же ограничения: синтетический временной ряд детектора теплового движения "), R("не содержит", { bold: true }), R(" — полон именно аналитический бюджет, а не синтетика.")]));
  K.push(links([ xr("Физика обратной задачи → §4.5","s_phys_e2e"), sep(), xr("Реализация калибровки → §7.8","s_impl_dsp"), sep(), xr("Реализация NEA → §7.9","s_impl_nea"), sep(), flink("src/optivibe/dsp/nea.py"), sep(), flink("src/optivibe/mechanics/thermal.py") ]));

  K.push(H(3,"6.5-бис Причинный потоковый слой (реальное время)","s_algo_stream"));
  K.push(para([R("Всё описанное в §6.5 — "), R("батчевая", { bold: true }), R(" цепочка: она видит запись целиком. Частотный интегратор берёт преобразование всей записи, временной — детренд по всей длине и фильтр с нулевой фазой. Ни один не может выдать отсчёт, пока запись не закончилась, поэтому реальному времени нужен отдельный, "), R("причинный", { bold: true }), R(" слой ("), flink("src/optivibe/dsp/streaming.py","dsp/streaming.py"), R("), а не «тот же код по кусочкам».")]));
  K.push(...bullets([
    [c("LeakyIntegrator"), R(" — рекурсивный интегратор 1-го порядка со срезом f_c, несущий состояние фильтра между кадрами. Две ступени: a→v и v→x. Срез задаётся "), c("f_c_stream"), R(" и "), R("намеренно независим", { bold: true }), R(" от батчевого f_hp: причинный ФВЧ и нуль-фазовая маска ведут себя по-разному у нижнего края полосы, поэтому раздельная настройка подгоняет согласие поток↔батч, не трогая батч.")],
    [c("StreamingSpectrum"), R(" — кольцевой буфер плюс экспоненциальное усреднение периодограмм; посегментно вызывается "), R("тот же", { bold: true }), R(" оценщик PSD, что и в батче, поэтому нормировка идентична (одна реализация метрик, 17 §7), а выход совместим с оценкой доминант и полосного СКЗ.")],
    [c("StreamingDsp"), R(" — оркестратор пяти стадий с состоянием: калибровка блоком (та же функция, что в батче — "), R("бит-в-бит", { bold: true }), R(") → два leaky-интегратора → бегущие спектры → бегущее СКЗ → снимок "), c("VibrationResult"), R(". NEA считается один раз: он сигнал-независим.")],
  ]));
  K.push(para([R("Что именно проверено (и что "), R("не", { bold: true }), R(" проверяется). Бит-идентичность потока и батча "), R("на стадии интеграторов невозможна по конструкции", { bold: true }), R(" — батч непричинен. Поэтому приёмка формулируется тремя пунктами: калибровка поток ≡ батч бит-в-бит; результат не зависит от размера кадра (швонезависимость, бит-в-бит после прогрева); метрики в полосе сходятся с батчем "), R("и с аналитикой", { bold: true }), R(" v = A/ω, x = A/ω² в пределах допусков 11 §7 — после прогрева. Пункта «интеграторы поток ≡ батч бит-в-бит» в приёмке нет, и это не послабление, а следствие причинности. Вывод — theory-06 §3.6/§5/§7.6 ("), flink("docs/theory/06_dsp_algorithm.md","docs/theory/06_dsp_algorithm.md"), R("), здесь он не пересказывается.")]));
  K.push(para([R("Отсюда — "), c("warmed"), R(": пока причинные фильтры не осели, числа "), R("не окончательны", { bold: true }), R(" (сильнее всего это видно на перемещении — двойное интегрирование даёт самый долгий переходный процесс). Флаг производит расчётный слой и публикует интерфейс (§"), xr("3.6","s_live"), R("). Оттуда же — деление ручек на «батч-только» и «оба»: в реальном времени нуль-фазового интегратора, преобразования по всей длине записи и её сегментации по Уэлчу "), R("просто не существует", { bold: true }), R(", а не «пока не сделано» (§"), xr("3.7","s_cmp"), R(").")]));
  K.push(links([ xr("Живой режим → §3.6","s_live"), sep(), xr("Реализация потока → §7.11","s_impl_stream"), sep(), flink("src/optivibe/dsp/streaming.py"), sep(), R("вывод: theory-06 §3.6/§5/§7.6",{size:18,color:MUT}) ]));

  K.push(H(2,"6.6 Переключаемая модель чувствительности","s_algo_sens"));
  K.push(para([R("Реестр "), c("SENSITIVITY_REGISTRY"), R(" (документ 14 §4, проектное решение SW-33) делает калибровку расширяемой по нескольким ортогональным осям: "), c("static"), R(" (умолчание = v1, скаляр на номинальном bias), "), c("operating_point"), R(" (пересчёт на рабочий bias по правилу η₀/η_peak = 0.37), "), c("nonlinear_curve"), R(" (поточечная инверсия монотонной ветви η(Δx) для >50g). Флаги осей: "), c("calibration∈{ideal,bench}"), R(", "), c("sensitivity_freq∈{plateau,dynamic}"), R(". Сигнатура "), c("SensitivityModel.at(state, freq_hz=None)"), R(" вектор-готова (3D-инверсия — без смены сигнатуры). Умолчания static+ideal+plateau воспроизводят тракт v1 бит-в-бит.")]));
  K.push(links([ xr("Реализация → §7.8","s_impl_dsp"), sep(), flink("src/optivibe/dsp/sensitivity.py") ]));

  K.push(H(2,"6.7 Аналитика: бюджеты, развёртки, Монте-Карло","s_algo_analysis"));
  K.push(...bullets([
    [c("truth_vs_recovery"), R(" — прогон forward+inverse и сравнение восстановленного a с известным входом: бюджет ошибки (амплитуда, фаза, спектр, доминанты, СКЗ); согласование ошибки у пола с NEA.")],
    [c("nea_budget"), R(" — спектр NEA(f) и полнополосный NEA, разложение по вкладам (дробовой / RIN / Джонсон / тепловой), полы скорости и перемещения, сверка с аналитикой. Тепловая ветвь показывается в том же (ускорительном) домене; для вариантов B и C она косметическая, для A даёт +2.3 %, а резонансный D по существу тепло-лимитен.")],
    [c("parameter_sweep"), R(" — развёртки design по {L, R_c, P, bias, FS, вариант} и response по {амплитуда 0.1g→>50g, частота} (насыщение/THD/срыв линейности).")],
    [c("monte_carlo"), R(" — разбросы допусков (заделка→Q, R_c, зазор, bias) + технологическая децентровка ε_x как случайный параметр прогона; статистика NEA и перекрёстной оси; воспроизводимо по сиду.")],
  ]));
  K.push(links([ xr("Семейство вариантов → §4.6","s_phys_family"), sep(), flink("src/optivibe/analysis/"), sep(), R("CLI: ",{size:18,color:MUT}), c("optivibe sweep"), R(" / ",{size:18,color:MUT}), c("optivibe report") ]));

  K.push(H(2,"6.8 Ожидаемые пики: что спектр обязан показать","s_algo_peaks"));
  K.push(para([R("Слой отвечает на вопрос, который задают "), R("до", { bold: true }), R(" появления данных: при такой композиции датчика и таком стимуле — какие линии прибор обязан показать и насколько высокими они должны быть, чтобы иметь значение. Расчёт идёт "), R("только по конфигурации", { bold: true }), R(": функция принимает сценарий и вариант, временных рядов не касается вовсе, поэтому предсказание одинаково доступно CLI, интерфейсу и тесту, а интерфейс не может показать маркер, которого нет в API.")]));
  K.push(para([R("Измеренное и ожидаемое — "), R("разные эпистемические категории", { bold: true }), R(", поэтому предсказание оформлено отдельным артефактом "), c("ExpectedPeaks"), R(", а не полем результата обратной задачи: результат несёт то, что цепочка "), R("восстановила из записи", { italics: true }), R(", предсказание вычислимо вообще без записи. Заодно замороженный контракт результата остался нетронутым.")]));
  K.push(tbl(
    ["Ветвь таксономии","Где ожидается линия","Статус в v1"],
    [
      ["mode","f₁ консоли и полоса f₁/Q вокруг неё","реализована; f₁ считается моделью механики, а не по реперу 100/L²; ширина берёт эффективную Q прогона, включая override"],
      ["harmonic","k·f тонов сценария — от нелинейности η(Δx)","реализована; высота 2f из HD2 = |η″|·d/(4|η′|) при d = |H_lat(f)|·a"],
      ["intermod","f_i ± f_j для пар тонов","реализована (позиции)"],
      ["sideband","f_c ± k·f_m модулированной несущей","реализована: АМ m·a_c/2, ЧМ a_c·|J_k(β_FM)|"],
      ["mains","50·k Гц (наводка сети)","ветвь объявлена, предиктора нет"],
      ["alias","зеркала линий выше Найквиста","ветвь объявлена, предиктора нет"],
      ["f_mount","резонансы оснастки/подвеса","ветвь объявлена, предиктора нет"],
    ],
    [1450,3500,4410]
  ));
  K.push(para([R("Таксономия "), R("открытая", { bold: true }), R(": все семь ветвей объявлены сразу, а запрос нереализованной даёт пустой результат, а не исключение — потребитель всегда может запросить весь список. Обратная дисциплина соблюдена не менее строго: "), R("высота линии f₁ не заявляется", { bold: true }), R(" (это усиление |D(f₁)| = Q того, что окажется на f₁, — свойство входа, а не конфигурации), так же не заявляются высоты 3f и IM2, а признак значимости возвращает «не могу сказать», когда высота или разрешение неизвестны, — вместо удобного умолчания.")]));
  K.push(para([R("Порог значимости — "), R("числовой, а не глазомерный", { bold: true }), R(": он собирается из бюджета NEA по той же конфигурации (то же выражение постоянного тока, что в стадии детектора; конвенция балансного/опорного канала пробрасывается, а не выбирается заново) и переводится в домен амплитудного спектра как k·√(2·Δf)·NEA(f). Разрешение Δf = fs/N берётся из спеки возбуждения; для файловых видов длительность из конфига неизвестна, поэтому разрешение и порог честно возвращаются пустыми — без подгонки. Линии выше Найквиста не предсказываются: это ветвь alias, а не mode/harmonic.")]));
  K.push(H(3,"Потребитель: протокол интерпретации спектра","s_algo_peaks_use"));
  K.push(para([R("Слой обслуживает шаг 1 протокола 20 §3 — «предсказать ожидаемое» перестаёт быть ручной операцией и делается "), R("до выезда", { bold: true }), R(", без доступа к прибору; на стенде остаётся шаг 2 — вычесть известное. Три уровня потребления: маркеры и затенённая полоса f₁/Q на фигуре спектра (стилистически отличные от измеренных доминант — различать их и есть смысл протокола), печать списка в "), c("optivibe report"), R(" с сохранением "), c("spectrum.png"), R(", и опт-ин оверлей на живом спектре в интерфейсе.")]));
  K.push(para([R("Практическая поправка, которую стоит знать заранее (20 §3.2a). Для боковых ЧМ инженерное "), R("правило Карсона", { bold: true }), R(" (k_max ≈ β_FM + 1) — эвристика "), R("удержания мощности", { bold: true }), R(" (~99 %), а не порог "), R("видимости", { bold: true }), R(" линии над шумовым полом. При β_FM = 3 Карсон даёт k_max ≈ 4, тогда как |J₅| = 0.043, |J₆| = 0.011, |J₇| = 0.0026 — на низком поле варианта B эти линии наблюдаемы, и предсказатель, отсекающий по порогу NEA, доходит до k = 7. Следствие для реальных записей: линии за полосой Карсона — "), R("не артефакт", { bold: true }), R(" и не «лишний» пик, а корректная категория «вносимое стимулом». Порог видимости определять от измеренного пола записи, а не от эвристики.")]));
  K.push(links([ xr("Композит и модуляция → §6.1","s_algo_composite"), sep(), xr("Живой режим → §3.6","s_live"), sep(), xr("Реализация → §7.12","s_impl_peaks"), sep(), flink("src/optivibe/analysis/expected_peaks.py"), sep(), R("база знаний: 20 §3.1/§3.2/§3.2a, 07",{size:18,color:MUT}) ]));


  return K;
}

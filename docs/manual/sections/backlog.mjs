// docs/manual/sections/backlog.mjs
// AUTO-SLICED (S-DOC-2) from manual_build_doc.js lines 748..761; content verbatim.
// Owns its bookmarks/links. Edit this module only; build.mjs orders it via manifest.mjs.
import {
  R, c, P, para, H, xr, flink, ext, links, sep, code, cap, bullets, nums, img, tbl, fml,
  AlignmentType, Paragraph, TextRun, PageBreak,
  REPO, FIG, INK, MUT, ACC, ACCD, LINE, CW,
} from "../shared.mjs";

export function section() {
  const K = [];
  K.push(H(1,"12. Открытые вопросы и направления расширения","ch12"));
  K.push(para("Эти пункты не влияют на работу v1, но полезны для понимания границ модели и планов (база знаний: документы 13 §открытые вопросы, 14 §8)."));
  K.push(para("Закрыто в серии M-01/M-02/M-12/M-10/M-09: вычислимая модель Q(L) (Q больше не константа); тепловой пол NEA_th (четвёртая ветвь бюджета); форма линии источника и вывод видности/RIN из спектра; поляризация — закрыта как допущение с числовыми границами (не моделируется осознанно)."));
  K.push(para([R("Закрыто следующей серией (S-13 → S-22): "), R("анализатор реального выхода прибора", { bold: true }), R(" ("), c("optivibe analyze"), R(" — обработка захватов фотодетектора минуя forward); "), R("реал-тайм осциллограф", { bold: true }), R(" (причинный потоковый слой и живой режим — §6.5-бис, §3.6); "), R("ввод измеренных характеризационных артефактов", { bold: true }), R(" (S-13/M-15 — CSV с сайдкаром, провенанс и неопределённости, §3.5); формы отражателя sphere/plane/wedge (§4.7); композит и АМ/ЧМ-возбуждение (§6.1); предсказание ожидаемых пиков (§6.8); стенд сравнения цепочек ЦОС (§3.7). Ниже — то, что "), R("действительно", { italics: true }), R(" остаётся открытым.")]));
  K.push(...bullets([
    [R("Главное: фаза 0 не проведена. ",{bold:true}), R("Реализован "), R("тракт", { bold: true }), R(" ввода измерений, а не сами измерения: конфигурация прототипа держит плейсхолдеры, а сверка модели с реальным прибором (уровень L3 плана валидации) "), R("не пройдена", { bold: true }), R(". Живой режим, ожидаемые пики и стенд сравнения к ней не приближают — это экраны над той же моделью.")],
    [R("Полная 3D/вектор-инверсия и live-калибровка по стенду: ",{bold:true}), R("швы заложены (оси A/D сигнатуры чувствительности), "), c("calibration=bench"), R(" остаётся хуком.")],
    [R("parquet-вывод и метаданные экспорта: ",{bold:true}), R("точка расширения за реестром.")],
    [R("Живое параллельное сравнение потоковых цепочек (W-2b): ",{bold:true}), R("сейчас стенд сравнивает цепочки на записи; сравнение двух причинных цепочек "), R("в потоке", { italics: true }), R(" требует второго потребителя в цикле живого режима — отдельная задача.")],
    [R("Асимметрия окна (M-21): ",{bold:true}), R("выбранное окно доходит до PSD (а через неё до полосного СКЗ и оценки ISO) и до потокового спектра, но "), R("не", { bold: true }), R(" до амплитудного спектра при одиночном преобразовании. Проброс сдвинул бы доминанты приёмок и все эталонные тесты, поэтому это санкционируемая смена контракта, а не правка; асимметрия названа в подсказке к полю.")],
    [R("Осознанные пустоты живого режима (S-24, W-4): ",{bold:true}), R("кривая входа и панель детектора в потоке пусты (у потока нет истины, выровненной по окну), бесконечный стимул получается зацикливанием с "), R("помеченным", { italics: true }), R(" стыком. Честная фазовая непрерывность и досводка инлайн-справки — в планах.")],
    [R("Нереализованные ветви таксономии пиков: ",{bold:true}), R("mains (50·k Гц), alias (зеркала выше Найквиста) и f_mount (резонансы оснастки) объявлены, но предикторов не имеют — запрос даёт пустой результат, а не ошибку.")],
    [R("Тематический пере-раскрой руководства (S-11): ",{bold:true}), R("сейчас документ нарезан по главам; целевая раскладка — модули по подсистемам.")],
    [R("Q_air(p) — добротность при пониженном давлении (M-13): ",{bold:true}), R("режим vacuum: true — идеализация (Q_air = ∞); реальное разрежение проходит три кнудсеновских режима. Количественные вакуумные выводы давать нельзя до реализации модели.")],
    [R("Гистерезисные потери и нижняя декада варианта A (M-14): ",{bold:true}), R("угол потерь кварца на суб-герцах не установлен; тепловой пол там может подняться выше оптического. Плоская (вязкая) модель канонизирована для v1.")],
    [R("Синтетика теплового движения: ",{bold:true}), R("тепловой пол полон в аналитическом бюджете NEA, но во временном ряде детектора броуновского движения консоли нет — записанная петля.")],
    [R("Ускорение (numba/torch): ",{bold:true}), R("по результатам профилирования, за интерфейсом реализации.")],
  ]));
  K.push(para([R("Дорожная карта и журнал решений — документы 12 и 13 базы знаний, бэклог с приоритетами — документ 16, текущий слепок репозитория — документ 14. Разделение важно при чтении: сделанное описано в главах 3–9 настоящего руководства, планируемое — только здесь, и смешивать одно с другим не следует.")]));
  K.push(links([ xr("Семейство и аналитика → §4.6 / §6.7","s_phys_family"), sep(), xr("Границы верифицированного → §3.7","s_cmp_verdict"), sep(), flink("docs/"), sep(), ext("репозиторий проекта","https://github.com/SuperMarioFernandess/optivibe") ]));


  return K;
}

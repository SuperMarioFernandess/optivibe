// docs/manual/sections/physmodel.mjs
// AUTO-SLICED (S-DOC-2) from manual_build_doc.js lines 207..265; content verbatim.
// Owns its bookmarks/links. Edit this module only; build.mjs orders it via manifest.mjs.
import {
  R, c, P, para, H, xr, flink, ext, links, sep, code, cap, bullets, nums, img, tbl, fml,
  AlignmentType, Paragraph, TextRun, PageBreak,
  REPO, FIG, INK, MUT, ACC, ACCD, LINE, CW,
} from "../shared.mjs";

export function section() {
  const K = [];
  K.push(H(1,"4. Физическая модель реального прибора","ch4"));
  K.push(para([R("Алгоритмы ПО опираются на физическую модель из базы знаний проекта (документы 00–08). Эта глава кратко излагает модель; подробные выводы формул и проверки — в указанных документах. Связи «физика → алгоритм → код» приведены в строках «↳ связи» и в "), xr("Карте связей (глава 10)","ch10"), R(".")]));

  K.push(H(2,"4.1 Принцип действия и геометрия","s_phys_over"));
  K.push(para("Отрезок одномодового волокна (плавленый кварц, D = 125 мкм) защемлён одним концом (z = 0) и свободен на другом (z = L) — это консоль. Внешнее ускорение изгибает консоль; торец смещается на Δx и наклоняется на θy. Свет (1550 нм) выходит из торца, проходит воздушный зазор A, отражается от выпуклого цилиндрического зеркала и частично вводится обратно в волокно. Смещение торца меняет эффективность обратного ввода η, а с ней — мощность на фотодетекторе. Цилиндр делает отклик направленным: чувствительность к целевой оси x (в плоскости кривизны) и подавление поперечной оси y."));
  K.push(fml("a(t) →[механика] (Δx, Δy, Δz, θx, θy) →[оптика] η →[ ℛP ] S = I_детектора"));
  K.push(para("Вектор состояния торца (Δx, Δy, Δz, θx, θy) — единый межподсистемный интерфейс (ICD, документ 04); вокруг него построены все контракты данных кода."));

  K.push(H(2,"4.2 Механика консоли","s_phys_mech"));
  K.push(para("Изгиб описывается уравнением Эйлера–Бернулли (документ 02). Свободные колебания дают собственные частоты и формы мод; при базовом возбуждении ускорением a(t) распределённая сила инерции изгибает балку."));
  K.push(fml("ωₙ = (βₙL)² · √(EI / ρSL⁴),   β₁L = 1.8751   ⇒   f₁ ≈ 100/L²  кГц (L в мм)"));
  K.push(...img("mode.png",420,240,"Рис. 1. Форма первой моды консоли; наклон у торца относительно прогиба = 1.377 (связь θ–Δ)."));
  K.push(...img("f1L.png",420,240,"Рис. 2. Собственная частота f₁ ∝ L⁻²: короткая консоль выносит резонанс за полосу. Точки — рабочие длины вариантов A/B/C."));
  K.push(para("Квазистатический прогиб торца (на «полке», f ≪ f₁) и напряжение у заделки:"));
  K.push(fml("w_tip = ρSaL⁴ / (8EI) ≡ H_lat^QS · a  (≈ 0.0384·L⁴ нм/g);   σ = 2ρaL²/R"));
  K.push(para("Динамика — отклик затухающего осциллятора моды 1; на резонансе усиление ≈ Q:"));
  K.push(fml("H_lat(f) = H_lat^QS · |D(f)|,   D(f) = 1 / [ 1 − (f/f₁)² + i·(f/f₁)/Q ]"));
  K.push(...img("hlat.png",430,245,"Рис. 3. Поперечная АЧХ |H_lat(f)|: полка при f ≪ f₁ и резонанс при f₁. Короткая консоль (C) держит резонанс вне полосы; L = 3 мм даёт резонанс внутри полосы."));
  K.push(para("Связь наклона и смещения торца (модальная): θ/Δx = 1.377/L. Эта константа передаётся в оптику аргументом, а сам наклон даёт механика через TipState."));
  K.push(para("Добротность Q — не число из таблицы. Начиная с версии, закрывшей задачу M-02 (решения R-47/R-48), Q вычисляется моделью Q(L) по трём независимым каналам потерь: вязкое трение о воздух (гидродинамическая функция круглого сечения, Sader 1998), потери в заделке (Q_anchor = 2.17·(L/D)³) и внутренние потери кварца. Обратные добротности складываются:"));
  K.push(fml("1/Q_total = 1/Q_air + 1/Q_anchor + 1/Q_struct + 1/Q_TED,   Q_air ∝ 1/L,   Q_anchor ∝ L³"));
  K.push(para("Практический вывод: Q(L) немонотонна — на коротких консолях лимитирует заделка, на длинных воздух, а максимум Q ≈ 2611 приходится на L ≈ 2.05 мм (это и есть длина варианта B). Значения для семейства: A (5.0 мм) 1297, B (2.0 мм) 2606, C (1.41 мм) 1969, D (4.47 мм) 1472. Базовая среда — воздух (≈1 атм, решение R-49); режим vacuum: true в коде сохранён, но это идеализация (Q_air = ∞) — количественные выводы в вакууме давать нельзя до реализации модели Q_air(p) (бэклог M-13)."));
  K.push(para("Тот же Q задаёт тепловой (броуновский) шумовой пол датчика — см. §4.4."));
  K.push(links([ xr("Алгоритм механики → §6.2","s_algo_mech"), sep(), xr("Реализация → §7.5","s_impl_mech"), sep(), flink("src/optivibe/mechanics/cantilever.py"), sep(), flink("src/optivibe/mechanics/damping.py"), sep(), R("база знаний: документ 02 §5, 07 §2.3; решения R-47/R-48/R-49",{size:18,color:MUT}) ]));

  K.push(H(2,"4.3 Оптика: эффективность ввода η","s_phys_opt"));
  K.push(para("Поле моды волокна — гауссов пучок (w₀ ≈ 5.2 мкм, z_R ≈ 54.8 мкм, θ₀ ≈ 94.9 мрад). Возвратное поле вводится в моду с эффективностью η — квадратом нормированного перекрытия (документ 03, Marcuse). Цилиндр факторизует η = η_x·η_y (плоскость кривизны × плоская плоскость). Рабочая точка задаётся смещением (bias) Δx₀ ≈ 2 мкм на крутом склоне."));
  K.push(fml("S = ℛP·(R₁ + ρη),   ∂η/∂Δx ≈ −0.16/мкм (эффективный, ∝ 1/R_c),   ∂η/∂Δy = 0 (1-й порядок)"));
  K.push(...img("eta.png",420,240,"Рис. 4. Оптическая связь η(Δx). Рабочая точка (bias) ставится на склоне; наклон в ней задаёт чувствительность. Без bias наклон равен нулю."));
  K.push(para("Плоская плоскость цилиндра в направлении y даёт строгий ноль 1-го порядка (перекрёстная по y подавлена; остаток квадратичен). Эффективный наклон по целевой оси усилен наклоном торца: множитель [1 + 1.377·(R_c+A)/L]; анизотропия ≈ L/(1.377·R_c). Маршрут 2 (широкополосный источник, SLD) «смывает» интерференцию скола, поэтому вклады складываются по интенсивности и схема нечувствительна к зазору Δz."));
  K.push(links([ xr("Алгоритм оптики → §6.3","s_algo_opt"), sep(), xr("Реализация → §7.6","s_impl_opt"), sep(), flink("src/optivibe/optics/cylinder.py"), sep(), R("база знаний: документ 03, 04",{size:18,color:MUT}) ]));

  K.push(H(2,"4.3-бис Источник: ширина линии, форма спектра и когерентный смыв","s_phys_src"));
  K.push(para("Смыв интерференции скола — не декларация, а вычисляемое условие (задачи M-01/M-10; решения R-46, R-55…R-57). Из ширины линии источника Δλ (FWHM) модель получает частотную ширину Δν, длину когерентности L_c и видность остаточной полосы V(A) на разности хода двойного прохода OPD = 2A:"));
  K.push(fml("Δν = c·Δλ/λ²,   L_c = (2ln2/π)·λ²/Δλ,   V(A) = |γ(2A/c)|  (Винер–Хинчин)"));
  K.push(para("Критерий маршрута 2 — V < 0.03. Он зависит от формы линии: для гауссовой V = 2^(−(2A/L_c)²) и требуется A ≥ 1.1246·L_c; для лоренцевой V = 2^(−4A/L_c) и требуется A ≥ 1.2647·L_c — на 12.5 % больше. Причина: пороговое значение 0.03 лежит за точкой пересечения кривых (A = L_c, V = 1/16), где тяжёлые хвосты лоренца спадают уже медленнее."));
  K.push(para("Что это значит на практике (λ = 1550 нм, Δλ = 60 нм ⇒ L_c = 17.67 мкм): номинальный зазор A = 30 мкм смыт при любой форме, но худший случай юстировки A − 10 мкм = 20 мкм при лоренцевых хвостах НЕ смыт (V = 0.043 > 0.03; гаусс даёт 0.029 ✔). Покрытие этого случая требует Δλ ≥ 67 нм. Окончательный ответ даёт измеренный спектр источника — для этого предусмотрен режим lineshape: measured (табличная трасса OSA)."));
  K.push(para("Из того же спектра выводится шумовой пол RIN: RIN = κ/Δν, где форм-фактор κ равен 2 (прямоугольно-эквивалентная конвенция, принятая по умолчанию), 1.3286 для гауссовой линии (−1.78 дБ) и 0.6366 для лоренцевой (−4.97 дБ). Универсально: RIN = 2/Δν_eff, где Δν_eff — шумоэквивалентная ширина. Дефолт κ = 2 — консервативная верхняя граница; указывать форму линии имеет смысл только осознанно."));
  K.push(para("Важная граница: вывод RIN из ширины линии справедлив только для теплового/ASE-света (SLD). Для DFB RIN задаётся релаксационной динамикой, и модель требует задать его явно (иначе — ошибка конфигурации)."));
  K.push(links([ xr("Настройка источника → §5.5","s_arch_config"), sep(), flink("src/optivibe/optics/source.py"), sep(), R("база знаний: документ 03 §f′, 07 §1.2; решения R-46, R-55, R-56, R-57",{size:18,color:MUT}) ]));

  K.push(H(2,"4.4 Детектор и шумы (NEA)","s_phys_det"));
  K.push(para("Фототок I = ℛP·(R₁ + ρη) регистрируется фотодиодом; постоянная составляющая I_DC и переменная I_AC = ℛPρ·Δη. Шумы (документ 07): дробовой (белый, СПМ 2e·I_DC), относительный шум интенсивности RIN (для SLD ≈ −126 дБ/Гц), джонсоновский шум трансимпеданса. Опорный/балансный канал обязателен: он подавляет синфазный RIN (CMRR ≳ 30 дБ), переводя прибор в дробово-ограниченный режим. Металлизация зеркала (ρ ≈ 0.98) обязательна."));
  K.push(para("Шумовой пол, приведённый ко входу — NEA (noise-equivalent acceleration), в мкg/√Гц. Ветвей четыре: три токовые (дробовая, RIN, джонсоновская) и четвёртая — тепловой механический (броуновский) пол консоли, который складывается с ними квадратурно уже в домене ускорения (задача M-12; решения R-52…R-54):"));
  K.push(fml("NEA(f) = i_n(f) / |s_target(f)|  ⊕  NEA_th,   NEA_th = √( 4k_BT·ω₁ / (Q·M_a) ),   M_a ≈ 0.58·ρSL"));
  K.push(para("Тепловой пол — не «поправка на всякий случай»: для вариантов B и C он косметический (доли процента), но для A даёт +2.3 %, а резонансный D по существу тепло-лимитен (NEA_th = 0.227 против оптических 0.381 мкg/√Гц). Ключевая тонкость — «ускорительная» эффективная масса M_a ≈ 0.58·ρSL (не кинетическая 0.243·ρSL: подстановка последней завысила бы пол в 1.55 раза). Резонанс в NEA_th не виден: он сокращается с резонансом самого сигнала — это точное свойство, а не приближение. Плоскость же пола в полосе — вязкое приближение; у варианта A в нижней декаде возможен подъём (открытый вопрос M-14)."));
  K.push(para("Как ветвь ведёт себя в ПО: она включена по умолчанию (include_thermal=True) и отключается флагом (тогда числа бит-в-бит совпадают с версией до M-12). При анализе РЕАЛЬНЫХ записей (роль «анализатор») тепловая ветвь намеренно НЕ добавляется: измеренный фототок уже содержит броуновское движение консоли, и добавка удвоила бы учёт."));
  K.push(...img("nea.png",430,245,"Рис. 5. Спектр NEA(f) (вариант B): дробовая компонента улучшается у резонанса; тепловой пол плоский; 1/f у нижнего края снимается chopper-стабилизацией."));
  K.push(para("Поляризация в версии 1 сознательно не моделируется (решение R-58) — это допущение с числовыми границами, а не пробел. Эффективность ввода η поляризационно-независима точно; схема общего пути (падающий и возвратный свет — по одному волокну) делает неприменимым классический polarization fading; остальные каналы дают ≤0.2 % и лежат на тепловых временах, то есть снимаются AC-связью. Степень поляризации входит в пол RIN множителем (1+DOP²)/2, и принятая конвенция соответствует полностью поляризованному свету — консервативной верхней границе, поэтому отдельный параметр DOP намеренно не вводится (иначе пол понижался бы «по предположению», а при измеренном RIN получился бы двойной учёт)."));
  K.push(para("Оговорка, которую следует помнить при настройке: значение cmrr_db = 40 дБ, стоящее в пресете балансного детектора по умолчанию, для статического баланса ОПТИМИСТИЧНО — SOP-зависимое деление ответвителя ограничивает подавление примерно 33 дБ. Требование ≥30 дБ выполняется, но штатное решение — авто-балансный приёмник; измеренное на стенде значение следует вписать в cmrr_db явно."));
  K.push(links([ xr("Алгоритм детектора → §6.4","s_algo_det"), sep(), xr("Бюджет NEA в DSP → §6.5","s_algo_dsp"), sep(), flink("src/optivibe/detector/photodiode.py"), sep(), flink("src/optivibe/mechanics/thermal.py"), sep(), R("база знаний: документ 07 §1–§2; решения R-52…R-54, R-58",{size:18,color:MUT}) ]));

  K.push(H(2,"4.5 Сквозная передаточная и обратная задача","s_phys_e2e"));
  K.push(para("Композиция механики и оптики даёт сквозную чувствительность (документ 05):"));
  K.push(fml("s_target(f) = ℛP·ρ·(∂η/∂Δx)_эфф · H_lat^QS · |D(f)|   [ А/(м/с²) ]"));
  K.push(para("Обратная задача (DSP прибора): из I_AC делением на s_target восстанавливается ускорение a, затем интегрированием — скорость v и виброперемещение x. Важная тонкость: при двойном интегрировании НЧ-шум усиливается (PSD_x = PSD_a/ω⁴), поэтому ошибка восстановления a мала, а шумовой пол перемещения x повышен — в отчётах эти величины разделяются. Каноническая величина для SNR — I_AC = ℛPρ·Δη; множитель между Δη/η₀ и I_AC/I_DC равен 1 + R₁/(ρη₀) (≈ 1.15 для металла)."));
  K.push(links([ xr("Алгоритм DSP → §6.5","s_algo_dsp"), sep(), xr("Реализация калибровки → §7.8","s_impl_dsp"), sep(), flink("src/optivibe/dsp/calibration.py"), sep(), R("база знаний: документ 05, 07",{size:18,color:MUT}) ]));

  K.push(H(2,"4.6 Семейство вариантов A/B/C/D","s_phys_family"));
  K.push(para("Полоса и полная шкала — проектные входы, поэтому из одной платформы получается семейство band-specific вариантов (документ 08). Главный вывод: сужение полосы вдвое снижает достижимый NEA вчетверо (мастер-закон NEA ∝ f_max²)."));
  K.push(...img("family.png",430,252,"Рис. 6. Семейство A/B/C/D на плоскости (полоса, NEA) и классы применений."));
  K.push(tbl(
    ["Вариант","Диапазон","L, мм","NEA (полка)","Класс / задачи"],
    [
      ["A","0.1–100 Гц","5.0","0.1–0.5 мкg/√Гц","прецизионный: сейсмика, инфразвук"],
      ["B","1 Гц–10 кГц","2.0","≈10.6 мкg/√Гц","индустриальный: вибромониторинг машин"],
      ["C","10 Гц–20 кГц","1.41","24–43 / ≈11 мкg/√Гц","широкополосный/ВЧ: акустика, диагностика"],
      ["D","линия f_line","√(100/f_line)","до десятков нг","резонансный (опц.): известная частота"],
    ],
    [1100,1700,1300,2360,2900]
  ));
  K.push(para([R("Числа FS и целевого NEA — проектные параметры; в коде варианты заданы в "), flink("configs/variants/A.yaml","configs/variants/{A,B,C,D}.yaml"), R(" (зеркало документа 08).")]));
  K.push(links([ xr("Конфигурация вариантов → §5.5","s_arch_config"), sep(), xr("Развёртки и Монте-Карло → §6.7","s_algo_analysis"), sep(), flink("configs/variants/B.yaml") ]));


  return K;
}

// docs/manual/sections/00_intro.mjs
// AUTO-SLICED (S-DOC-2) from manual_build_doc.js lines 74..91; content verbatim.
// Owns its bookmarks/links. Edit this module only; build.mjs orders it via manifest.mjs.
import {
  R, c, P, para, H, xr, flink, ext, links, sep, code, cap, bullets, nums, img, tbl, fml,
  AlignmentType, Paragraph, TextRun, PageBreak,
  REPO, FIG, INK, MUT, ACC, ACCD, LINE, CW,
} from "../shared.mjs";

export function section() {
  const K = [];
  K.push(H(1,"1. Введение и назначение","ch1"));
  K.push(para("OptiVibe — это цифровой двойник волоконно-оптического датчика вибрации (ВОД): программный комплекс, который физически достоверно воспроизводит работу прибора от входной вибрации до отсчётов фотодетектора и далее восстанавливает вибрацию обратно, как это делает прошивка реального устройства. Документ описывает физическую модель, алгоритмы, их реализацию в коде, а также работу пользователя с приложением, и связывает эти уровни между собой перекрёстными ссылками."));
  K.push(para("Продукт решает три задачи (три слоя ценности):"));
  K.push(...bullets([
    [R("Прямая модель (forward) — ",{bold:true}), R("симулятор сигнала: вибрация → механика консоли → оптика (эффективность ввода η) → фототок → детектор/АЦП с шумами → «сырые» отсчёты.")],
    [R("Обратная задача (inverse / DSP) — ",{bold:true}), R("то, что делает ПО прибора: из отсчётов → калибровка → ускорение целевой оси → интегрирование в скорость и виброперемещение → спектры, доминирующие частоты, СКЗ, виброоценка по ISO.")],
    [R("Аналитика и демонстрация — ",{bold:true}), R("бюджет шумов (NEA), развёртки параметров, Монте-Карло допусков, сравнение «истинное vs восстановленное», и десктоп-приложение для показа.")],
  ]));
  K.push(para("Ключевая особенность для специалистов: код доказуемо согласован с аналитической физической моделью — каждая подсистема проверяется golden-тестами против реперных значений физической базы знаний (документы 00–08). Архитектура «чистое ядро + сменные реализации через реестр + строгие контракты данных» позволяет наращивать функционал без переписывания физики."));
  K.push(H(3,"Как читать документ и его связи","s_intro_links"));
  K.push(para("Документ разбит на тематические главы. Одна и та же подсистема (механика, оптика, детектор, DSP) описана на трёх уровнях, и эти уровни связаны ссылками:"));
  K.push(...bullets([
    [R("физический принцип и формулы — глава "), xr("4 «Физическая модель»","ch4"), R(";")],
    [R("алгоритм (что и как считается) — глава "), xr("6 «Алгоритмы»","ch6"), R(";")],
    [R("реализация (блоки кода и файлы) — глава "), xr("7 «Реализация в коде»","ch7"), R(".")],
  ]));
  K.push(para([R("В конце каждой подсистемы есть строка «↳ связи» с переходами между этими уровнями и ссылками на исходные файлы репозитория. Сводная "), xr("Карта связей (глава 10)","ch10"), R(" собирает всё в одну таблицу. Ссылки вида "), flink("src/optivibe/mechanics/cantilever.py"), R(" ведут на файлы в GitHub-репозитории; ссылки вида «документ 02 §3.2» — на физическую базу знаний проекта.")]));

  return K;
}
